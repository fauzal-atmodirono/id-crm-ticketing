diff --git a/app/javascript/dashboard/components-next/sidebar/Sidebar.vue b/app/javascript/dashboard/components-next/sidebar/Sidebar.vue
index d216af4..37c5fb6 100644
--- a/app/javascript/dashboard/components-next/sidebar/Sidebar.vue
+++ b/app/javascript/dashboard/components-next/sidebar/Sidebar.vue
@@ -71,7 +71,7 @@ const isFeatureEnabledonAccount = useMapGetter(
 // had to change shape -- only what it reads changed. `isSuperadmin` (0073)
 // comes from the same module-level singleton, so calling useCustomFeatures()
 // a second time here is free -- it does not refetch.
-const { hasFeature: protonHasFeature } = useCustomFeatures();
+const { hasFeature: protonHasFeature, t: tTerm } = useCustomFeatures();
 const protonAiAssistEnabled = computed(() => protonHasFeature('ai_assist'));
 const { hasPermission: protonHasPermission } = useProtonPermissions();
 const { isSuperadmin } = useCustomFeatures();
@@ -694,7 +694,7 @@ const menuItems = computed(() => {
           {
             name: 'RsaIncidents',
             icon: 'i-lucide-truck',
-            label: 'RSA Incident Log',
+            label: `${tTerm('field_incident')} Log`,
             to: accountScopedRoute('proton_rsa_incidents'),
           },
         ]
diff --git a/app/javascript/dashboard/components/widgets/ProtonCasesTable.vue b/app/javascript/dashboard/components/widgets/ProtonCasesTable.vue
index b3c361f..c7eabc7 100644
--- a/app/javascript/dashboard/components/widgets/ProtonCasesTable.vue
+++ b/app/javascript/dashboard/components/widgets/ProtonCasesTable.vue
@@ -17,6 +17,7 @@
      and ProtonDealerEscalationSection.vue already use for this. -->
 <script>
 import { frontendURL, conversationUrl } from 'dashboard/helper/URLHelper.js';
+import { useCustomFeatures } from 'dashboard/composables/useCustomFeatures';
 
 const ALL = 'all';
 
@@ -37,6 +38,10 @@ export default {
       default: () => [],
     },
   },
+  setup() {
+    const { t } = useCustomFeatures();
+    return { t };
+  },
   data() {
     return {
       divisionFilter: ALL,
@@ -170,13 +175,17 @@ export default {
         </select>
       </div>
       <div>
-        <label class="block mb-1 text-xs text-n-slate-11">Dealer</label>
+        <label class="block mb-1 text-xs text-n-slate-11">{{ t('partner') }}</label>
         <select
           v-model="dealerFilter"
           class="px-3 py-2 text-sm border rounded-lg border-n-weak bg-n-alpha-black2 text-n-slate-12"
         >
           <option v-for="option in dealerOptions" :key="option" :value="option">
-            {{ option === 'all' ? 'All dealers' : option }}
+            {{
+              option === 'all'
+                ? `All ${t('partner', 'plural_lower')}`
+                : option
+            }}
           </option>
         </select>
       </div>
diff --git a/app/javascript/dashboard/routes/dashboard/settings/reports/ProtonCaseLifecycle.vue b/app/javascript/dashboard/routes/dashboard/settings/reports/ProtonCaseLifecycle.vue
index 8b8297e..9f13b43 100644
--- a/app/javascript/dashboard/routes/dashboard/settings/reports/ProtonCaseLifecycle.vue
+++ b/app/javascript/dashboard/routes/dashboard/settings/reports/ProtonCaseLifecycle.vue
@@ -4,6 +4,9 @@ import BarChart from 'shared/components/charts/BarChart.vue';
 import LineChart from 'shared/components/charts/LineChart.vue';
 import ProtonWipAgingSection from './components/proton/ProtonWipAgingSection.vue';
 import { getLifecycle } from 'dashboard/api/protonMetrics';
+import { useCustomFeatures } from 'dashboard/composables/useCustomFeatures';
+
+const { t } = useCustomFeatures();
 
 // ── Data ──────────────────────────────────────────────────────────────────────
 
@@ -247,7 +250,7 @@ const recentCases = computed(() => {
                 <th
                   class="text-left py-2 pr-4 font-medium text-n-slate-11 whitespace-nowrap"
                 >
-                  Dealer
+                  {{ t('partner') }}
                 </th>
                 <th
                   class="text-left py-2 pr-4 font-medium text-n-slate-11 whitespace-nowrap"
diff --git a/app/javascript/dashboard/routes/dashboard/settings/reports/ProtonDepartments.vue b/app/javascript/dashboard/routes/dashboard/settings/reports/ProtonDepartments.vue
index c2122db..638bcab 100644
--- a/app/javascript/dashboard/routes/dashboard/settings/reports/ProtonDepartments.vue
+++ b/app/javascript/dashboard/routes/dashboard/settings/reports/ProtonDepartments.vue
@@ -2,6 +2,9 @@
 import { ref, computed, onMounted } from 'vue';
 import BarChart from 'shared/components/charts/BarChart.vue';
 import { getDepartments, getCallCentre } from 'dashboard/api/protonMetrics';
+import { useCustomFeatures } from 'dashboard/composables/useCustomFeatures';
+
+const { t } = useCustomFeatures();
 
 // ── Data ──────────────────────────────────────────────────────────────────────
 
@@ -254,7 +257,7 @@ const sortedCategoryByVehicleModel = computed(() =>
                 <th
                   class="text-left py-2 pr-4 font-medium text-n-slate-11 whitespace-nowrap"
                 >
-                  Dealer
+                  {{ t('partner') }}
                 </th>
                 <th
                   class="text-left py-2 pr-4 font-medium text-n-slate-11 whitespace-nowrap"
@@ -393,7 +396,7 @@ const sortedCategoryByVehicleModel = computed(() =>
         class="shadow outline-1 outline outline-n-container rounded-xl bg-n-solid-2 px-6 py-5"
       >
         <h3 class="text-sm font-semibold text-n-slate-12 mb-4">
-          Category × Vehicle Model
+          Category × {{ t('asset_model') }}
         </h3>
 
         <!-- Empty state -->
@@ -401,7 +404,7 @@ const sortedCategoryByVehicleModel = computed(() =>
           v-if="!sortedCategoryByVehicleModel.length"
           class="flex items-center justify-center h-24 text-sm text-n-slate-10"
         >
-          No category / vehicle-model data available
+          No category / {{ t('asset_model', 'lower') }} data available
         </div>
 
         <!-- Table -->
@@ -422,7 +425,7 @@ const sortedCategoryByVehicleModel = computed(() =>
                 <th
                   class="text-left py-2 pr-4 font-medium text-n-slate-11 whitespace-nowrap"
                 >
-                  Vehicle Model
+                  {{ t('asset_model') }}
                 </th>
                 <th
                   class="text-left py-2 pr-4 font-medium text-n-slate-11 whitespace-nowrap"
diff --git a/app/javascript/dashboard/routes/dashboard/settings/reports/ProtonWeeklyReport.vue b/app/javascript/dashboard/routes/dashboard/settings/reports/ProtonWeeklyReport.vue
index 6633164..5bb5a6c 100644
--- a/app/javascript/dashboard/routes/dashboard/settings/reports/ProtonWeeklyReport.vue
+++ b/app/javascript/dashboard/routes/dashboard/settings/reports/ProtonWeeklyReport.vue
@@ -40,6 +40,7 @@
 <script setup>
 import { ref, computed, onMounted, watch } from 'vue';
 import { useAlert } from 'dashboard/composables';
+import { useCustomFeatures } from 'dashboard/composables/useCustomFeatures';
 import V4Button from 'dashboard/components-next/button/Button.vue';
 import ReportHeader from './components/ReportHeader.vue';
 import ProtonScopeBadge from './components/proton/ProtonScopeBadge.vue';
@@ -62,6 +63,8 @@ import {
   formatDelta,
 } from './components/proton/reportScope';
 
+const { t } = useCustomFeatures();
+
 const TOP_N = 10;
 
 // ── Week picker ──────────────────────────────────────────────────────────
@@ -584,10 +587,12 @@ onMounted(() => {
         creation clock (reopen count is tracked separately).
       </li>
       <li>
-        <strong class="text-n-slate-12">Dealer-escalation turnaround</strong>
-        is measured from the moment a case is stamped as escalated to a dealer,
-        not from case creation — time spent unescalated does not count against a
-        dealer.
+        <strong class="text-n-slate-12"
+          >{{ t('partner') }}-escalation turnaround</strong
+        >
+        is measured from the moment a case is stamped as escalated to a
+        {{ t('partner', 'lower') }}, not from case creation — time spent
+        unescalated does not count against a {{ t('partner', 'lower') }}.
       </li>
       <li>
         <strong class="text-n-slate-12">Work-in-progress / aging</strong>
@@ -906,14 +911,14 @@ onMounted(() => {
       </p>
 
       <h4 class="text-xs font-semibold text-n-slate-11 mb-2">
-        Reopened cases by dealer (top {{ TOP_N }})
+        Reopened cases by {{ t('partner', 'lower') }} (top {{ TOP_N }})
       </h4>
       <div v-if="reopenRows.length" class="overflow-x-auto mb-6">
         <table class="w-full text-sm text-left">
           <thead>
             <tr class="border-b border-n-container">
               <th class="pb-2 pr-4 text-xs font-medium text-n-slate-11">
-                Dealer
+                {{ t('partner') }}
               </th>
               <th
                 class="pb-2 pr-4 text-xs font-medium text-n-slate-11 text-right"
@@ -957,7 +962,7 @@ onMounted(() => {
       </p>
 
       <h4 class="text-xs font-semibold text-n-slate-11 mb-2">
-        Category × vehicle model (top {{ TOP_N }})
+        Category × {{ t('asset_model', 'lower') }} (top {{ TOP_N }})
       </h4>
       <div v-if="categoryByVehicleModelRows.length" class="overflow-x-auto">
         <table class="w-full text-sm text-left">
@@ -967,7 +972,7 @@ onMounted(() => {
                 Category
               </th>
               <th class="pb-2 pr-4 text-xs font-medium text-n-slate-11">
-                Vehicle Model
+                {{ t('asset_model') }}
               </th>
               <th class="pb-2 pr-4 text-xs font-medium text-n-slate-11">
                 Case Type
diff --git a/app/javascript/dashboard/routes/dashboard/settings/reports/components/proton/ProtonDealerEscalationSection.vue b/app/javascript/dashboard/routes/dashboard/settings/reports/components/proton/ProtonDealerEscalationSection.vue
index 36b914e..57978bc 100644
--- a/app/javascript/dashboard/routes/dashboard/settings/reports/components/proton/ProtonDealerEscalationSection.vue
+++ b/app/javascript/dashboard/routes/dashboard/settings/reports/components/proton/ProtonDealerEscalationSection.vue
@@ -2,6 +2,9 @@
 import { ref, computed, onMounted } from 'vue';
 import { getDealerEscalation } from 'dashboard/api/protonMetrics';
 import ProtonScopeBadge from './ProtonScopeBadge.vue';
+import { useCustomFeatures } from 'dashboard/composables/useCustomFeatures';
+
+const { t } = useCustomFeatures();
 
 // `scopeStatus` is optional and unused by every existing embedding of this
 // component (SLAReports.vue) — default null means "render no badge",
@@ -30,7 +33,8 @@ onMounted(async () => {
     byDealer.value = result.by_dealer ?? [];
     slowestCases.value = result.slowest_cases ?? [];
   } catch (e) {
-    error.value = e.message || 'Failed to load dealer escalation data';
+    error.value =
+      e.message || `Failed to load ${t('partner', 'lower')} escalation data`;
   } finally {
     loading.value = false;
   }
@@ -56,7 +60,7 @@ function fmtDays(val) {
   >
     <div class="flex items-center gap-2 mb-4">
       <h3 class="text-sm font-semibold text-n-slate-12">
-        Dealer Escalation Turnaround
+        {{ t('partner') }} Escalation Turnaround
       </h3>
       <ProtonScopeBadge v-if="scopeStatus" :status="scopeStatus" />
     </div>
@@ -65,7 +69,7 @@ function fmtDays(val) {
     <woot-loading-state
       v-if="loading"
       class="text-xs"
-      message="Loading dealer escalation data…"
+      :message="`Loading ${t('partner', 'lower')} escalation data…`"
     />
 
     <!-- Error state -->
@@ -81,7 +85,7 @@ function fmtDays(val) {
       v-else-if="!sortedByDealer.length"
       class="flex items-center justify-center h-24 text-sm text-n-slate-10"
     >
-      No dealer escalation data available
+      No {{ t('partner', 'lower') }} escalation data available
     </div>
 
     <!-- Content -->
@@ -94,7 +98,7 @@ function fmtDays(val) {
               <th
                 class="pb-2 pr-4 text-xs font-medium text-n-slate-11 whitespace-nowrap"
               >
-                Dealer
+                {{ t('partner') }}
               </th>
               <th
                 class="pb-2 pr-4 text-xs font-medium text-n-slate-11 whitespace-nowrap text-right"
@@ -161,7 +165,7 @@ function fmtDays(val) {
                 <th
                   class="pb-2 pr-4 text-xs font-medium text-n-slate-11 whitespace-nowrap"
                 >
-                  Dealer
+                  {{ t('partner') }}
                 </th>
                 <th
                   class="pb-2 text-xs font-medium text-n-slate-11 whitespace-nowrap text-right"
diff --git a/app/javascript/dashboard/routes/dashboard/settings/reports/components/proton/ProtonWipAgingSection.vue b/app/javascript/dashboard/routes/dashboard/settings/reports/components/proton/ProtonWipAgingSection.vue
index a7a1888..d6027da 100644
--- a/app/javascript/dashboard/routes/dashboard/settings/reports/components/proton/ProtonWipAgingSection.vue
+++ b/app/javascript/dashboard/routes/dashboard/settings/reports/components/proton/ProtonWipAgingSection.vue
@@ -2,6 +2,9 @@
 import { ref, onMounted } from 'vue';
 import { getCaseAging } from 'dashboard/api/protonMetrics';
 import ProtonScopeBadge from './ProtonScopeBadge.vue';
+import { useCustomFeatures } from 'dashboard/composables/useCustomFeatures';
+
+const { t } = useCustomFeatures();
 
 // `scopeStatus` is optional and unused by every existing embedding of this
 // component (ProtonCaseLifecycle.vue) — default null means "render no
@@ -94,7 +97,7 @@ function fmtDays(val) {
             <th
               class="pb-2 pr-4 text-xs font-medium text-n-slate-11 whitespace-nowrap"
             >
-              Dealer
+              {{ t('partner') }}
             </th>
             <th
               class="pb-2 pr-4 text-xs font-medium text-n-slate-11 whitespace-nowrap"
diff --git a/app/javascript/dashboard/views/ProtonCasesPage.vue b/app/javascript/dashboard/views/ProtonCasesPage.vue
index 83ac453..a2b8287 100644
--- a/app/javascript/dashboard/views/ProtonCasesPage.vue
+++ b/app/javascript/dashboard/views/ProtonCasesPage.vue
@@ -18,6 +18,7 @@
      it's partial. -->
 <script>
 import { useAlert } from 'dashboard/composables';
+import { useCustomFeatures } from 'dashboard/composables/useCustomFeatures';
 import { fetchAllCases } from 'dashboard/api/protonCases';
 import ProtonCasesTable from 'dashboard/components/widgets/ProtonCasesTable.vue';
 import Banner from 'dashboard/components-next/banner/Banner.vue';
@@ -25,6 +26,10 @@ import Banner from 'dashboard/components-next/banner/Banner.vue';
 export default {
   name: 'ProtonCasesPage',
   components: { ProtonCasesTable, Banner },
+  setup() {
+    const { t } = useCustomFeatures();
+    return { t };
+  },
   data() {
     return {
       cases: [],
@@ -65,8 +70,8 @@ export default {
     <h1 class="mb-1 text-lg font-medium text-n-slate-12">Cases</h1>
     <p class="mb-6 text-sm text-n-slate-11">
       Every conversation as a case, filterable by division, case type, status,
-      channel, and dealer. Reads existing conversation data — there is nothing
-      to create or edit here.
+      channel, and {{ t('partner', 'lower') }}. Reads existing conversation
+      data — there is nothing to create or edit here.
     </p>
 
     <div v-if="loading" class="py-10 text-sm text-center text-n-slate-11">
diff --git a/app/javascript/dashboard/views/ProtonCustomer360Page.vue b/app/javascript/dashboard/views/ProtonCustomer360Page.vue
index fd89d5c..1519678 100644
--- a/app/javascript/dashboard/views/ProtonCustomer360Page.vue
+++ b/app/javascript/dashboard/views/ProtonCustomer360Page.vue
@@ -23,11 +23,16 @@
      exactly this second, independent label. -->
 <script>
 import { useAlert } from 'dashboard/composables';
+import { useCustomFeatures } from 'dashboard/composables/useCustomFeatures';
 import { searchCustomer360 } from 'dashboard/api/protonAdmin';
 import { frontendURL, conversationUrl } from 'dashboard/helper/URLHelper.js';
 
 export default {
   name: 'ProtonCustomer360Page',
+  setup() {
+    const { t } = useCustomFeatures();
+    return { t };
+  },
   data() {
     return {
       query: '',
@@ -89,16 +94,16 @@ export default {
   <div class="w-full h-full p-6 overflow-y-auto">
     <h1 class="mb-1 text-lg font-medium text-n-slate-12">Customer 360</h1>
     <p class="mb-6 text-sm text-n-slate-11">
-      Look up a customer by phone number or vehicle number to see their contact
-      record, cross-channel conversations, and RSA incident history in one
-      place.
+      Look up a customer by phone number or {{ t('asset_id', 'lower') }} to
+      see their contact record, cross-channel conversations, and
+      {{ t('field_incident', 'lower') }} history in one place.
     </p>
 
     <form class="flex max-w-xl gap-2 mb-8" @submit.prevent="search">
       <input
         v-model="query"
         type="text"
-        placeholder="Phone number or vehicle number"
+        :placeholder="`Phone number or ${t('asset_id', 'lower')}`"
         class="flex-1 px-3 py-2 text-sm border rounded-lg border-n-weak bg-n-alpha-black2 text-n-slate-12"
       />
       <button
@@ -172,9 +177,11 @@ export default {
       </div>
 
       <!-- RSA incidents (read-only) -->
-      <h2 class="mb-3 text-base font-medium text-n-slate-12">RSA incidents</h2>
+      <h2 class="mb-3 text-base font-medium text-n-slate-12">
+        {{ t('field_incident', 'plural') }}
+      </h2>
       <p v-if="!rsaIncidents.length" class="text-sm text-n-slate-11">
-        No RSA incidents found.
+        No {{ t('field_incident', 'plural_lower') }} found.
       </p>
       <div v-else class="overflow-x-auto border rounded-lg border-n-weak">
         <table class="w-full text-sm border-collapse text-n-slate-11">
@@ -182,9 +189,11 @@ export default {
             <tr class="text-left border-b border-n-weak text-n-slate-12">
               <th class="px-3 py-2 font-medium whitespace-nowrap">Date</th>
               <th class="px-3 py-2 font-medium whitespace-nowrap">
-                Vehicle no.
+                {{ t('asset_id') }}
+              </th>
+              <th class="px-3 py-2 font-medium whitespace-nowrap">
+                {{ t('asset_model') }}
               </th>
-              <th class="px-3 py-2 font-medium whitespace-nowrap">Model</th>
               <th class="px-3 py-2 font-medium whitespace-nowrap">Cause</th>
               <th class="px-3 py-2 font-medium whitespace-nowrap">
                 Purchased from
@@ -268,23 +277,27 @@ export default {
       <!-- DMS / TSP (optional — absent entirely unless the integration is
            enabled and wired in; see the module docstring above). -->
       <template v-if="dms">
-        <h2 class="mb-3 text-base font-medium text-n-slate-12">DMS / TSP</h2>
+        <h2 class="mb-3 text-base font-medium text-n-slate-12">
+          {{ t('partner_system') }}
+        </h2>
         <div
           v-if="dms.status !== 'ok'"
           class="max-w-xl px-4 py-3 text-sm border rounded-lg border-n-ruby-6 bg-n-ruby-2 text-n-ruby-11"
         >
-          Not connected — no live DMS/TSP data is available for this customer
-          right now.
+          Not connected — no live {{ t('partner_system') }} data is available
+          for this customer right now.
         </div>
         <template v-else>
           <div
             v-if="dms.mock"
             class="max-w-xl px-4 py-2 mb-3 text-xs font-medium border rounded-lg border-n-amber-6 bg-n-amber-2 text-n-amber-11"
           >
-            Mock data — for demo purposes only, not from a live DMS/TSP system.
+            Mock data — for demo purposes only, not from a live
+            {{ t('partner_system') }}.
           </div>
           <p v-if="!dms.customer" class="text-sm text-n-slate-11">
-            No DMS/TSP record found for this customer.
+            No {{ t('partner_system', 'lower') }} record found for this
+            customer.
           </p>
           <div
             v-else
@@ -295,9 +308,11 @@ export default {
           </div>
 
           <template v-if="dms.customer">
-            <h3 class="mb-2 text-sm font-medium text-n-slate-12">Vehicles</h3>
+            <h3 class="mb-2 text-sm font-medium text-n-slate-12">
+              {{ t('asset', 'plural') }}
+            </h3>
             <p v-if="!dms.vehicles.length" class="mb-4 text-sm text-n-slate-11">
-              No vehicles on file.
+              No {{ t('asset', 'plural_lower') }} on file.
             </p>
             <ul
               v-else
diff --git a/app/javascript/dashboard/views/ProtonDmsIntegrationPage.vue b/app/javascript/dashboard/views/ProtonDmsIntegrationPage.vue
index 0606e27..1a4abcf 100644
--- a/app/javascript/dashboard/views/ProtonDmsIntegrationPage.vue
+++ b/app/javascript/dashboard/views/ProtonDmsIntegrationPage.vue
@@ -38,6 +38,7 @@
 
 <script>
 import { useAlert } from 'dashboard/composables';
+import { useCustomFeatures } from 'dashboard/composables/useCustomFeatures';
 import {
   getDmsIntegration,
   putDmsIntegration,
@@ -103,6 +104,10 @@ const TEST_STATUS_META = {
 
 export default {
   name: 'ProtonDmsIntegrationPage',
+  setup() {
+    const { t } = useCustomFeatures();
+    return { t };
+  },
   data() {
     return {
       form: { ...EMPTY_FORM },
@@ -144,7 +149,7 @@ export default {
       } catch (err) {
         this.errored = true;
         useAlert(
-          'Failed to load the DMS/TSP integration: ' +
+          `Failed to load the ${this.t('partner_system')} integration: ` +
             (err.message || String(err))
         );
       } finally {
@@ -194,10 +199,10 @@ export default {
         this.form.auth_type = normalizeAuthType(this.form.auth_type);
         this.cancelReplaceCredential();
         this.testResult = null;
-        useAlert('DMS/TSP integration saved.');
+        useAlert(`${this.t('partner_system')} integration saved.`);
       } catch (err) {
         useAlert(
-          'Failed to save the DMS/TSP integration: ' +
+          `Failed to save the ${this.t('partner_system')} integration: ` +
             (err.message || String(err))
         );
       } finally {
@@ -226,17 +231,20 @@ export default {
 
 <template>
   <div class="w-full h-full p-6 overflow-y-auto">
-    <h1 class="mb-1 text-lg font-medium text-n-slate-12">DMS / TSP</h1>
+    <h1 class="mb-1 text-lg font-medium text-n-slate-12">
+      {{ t('partner_system') }}
+    </h1>
     <p class="mb-6 text-sm text-n-slate-11">
-      Configure the Dealer Management / Telematics Service Provider connection
-      used to look up vehicle and service history from Customer 360.
+      Configure the {{ t('partner_system', 'lower') }} connection used to
+      look up {{ t('asset', 'lower') }} and service history from
+      Customer 360.
     </p>
 
     <div v-if="loading" class="py-10 text-sm text-center text-n-slate-11">
       Loading…
     </div>
     <div v-else-if="errored" class="py-10 text-sm text-center text-n-slate-11">
-      Could not load the DMS/TSP integration.
+      Could not load the {{ t('partner_system', 'lower') }} integration.
     </div>
 
     <template v-else>
@@ -248,7 +256,7 @@ export default {
         {{
           form.enabled
             ? 'The integration is enabled but has no base URL configured, so it will not be used.'
-            : 'This integration is disabled — Customer 360 will not show any DMS/TSP data until it is enabled and configured.'
+            : `This integration is disabled — Customer 360 will not show any ${t('partner_system')} data until it is enabled and configured.`
         }}
       </div>
 
@@ -394,7 +402,8 @@ export default {
           />
           <p class="mt-1 text-xs text-n-slate-11">
             Between 0.1 and 30 seconds. A Customer 360 lookup waits at most
-            this long for the DMS/TSP before showing CRM data only.
+            this long for the {{ t('partner_system') }} before showing CRM
+            data only.
           </p>
         </div>
 
@@ -445,9 +454,10 @@ export default {
           and the provider's API documentation.
         </div>
         <div v-if="testResult.status === 'not_configured'" class="mt-1 text-xs">
-          Nothing was sent to the DMS/TSP — there's no saved configuration to
-          test yet. Fill in the base URL, click Replace next to Credential to
-          store one, then Save before testing again.
+          Nothing was sent to the {{ t('partner_system') }} — there's no
+          saved configuration to test yet. Fill in the base URL, click
+          Replace next to Credential to store one, then Save before testing
+          again.
         </div>
       </div>
     </template>
diff --git a/app/javascript/dashboard/views/ProtonEscalationRoutingPage.vue b/app/javascript/dashboard/views/ProtonEscalationRoutingPage.vue
index 64adc16..bea4d01 100644
--- a/app/javascript/dashboard/views/ProtonEscalationRoutingPage.vue
+++ b/app/javascript/dashboard/views/ProtonEscalationRoutingPage.vue
@@ -9,6 +9,7 @@
      key itself means delete + re-add. -->
 <script>
 import { useAlert } from 'dashboard/composables';
+import { useCustomFeatures } from 'dashboard/composables/useCustomFeatures';
 import {
   listPics,
   upsertPic,
@@ -103,6 +104,10 @@ function splitEmails(value) {
 
 export default {
   name: 'ProtonEscalationRoutingPage',
+  setup() {
+    const { t } = useCustomFeatures();
+    return { t };
+  },
   data() {
     return {
       pics: [],
@@ -117,8 +122,6 @@ export default {
       errored: false,
       savingPic: false,
       savingDealer: false,
-      dealerRoles: DEALER_ROLES,
-      ladderRungs: LADDER_RUNGS,
       ladderForm: { ...EMPTY_LADDER_FORM },
       ladderEffective: null,
       ladderEnvDefaults: null,
@@ -128,6 +131,35 @@ export default {
       loadingInFlight: false,
     };
   },
+  computed: {
+    // Built from the module-level role/rung tables on every render rather
+    // than baked into data() once, so a term dictionary that finishes
+    // loading after this component mounts (see useCustomFeatures'
+    // fail-open async load) still reaches these labels reactively instead
+    // of freezing them at their key-name fallback.
+    dealerRoles() {
+      const labels = {
+        cre: this.t('partner_rep'),
+        principal: this.t('partner_principal'),
+        owner: this.t('partner_owner'),
+      };
+      return DEALER_ROLES.map(role => ({
+        ...role,
+        label: labels[role.key] || role.label,
+      }));
+    },
+    ladderRungs() {
+      const hints = {
+        step2_hours: `${this.t('partner_rep')} is expected to reply by now. Nothing is sent.`,
+        step3_hours: `Emails the ${this.t('partner_principal')}.`,
+        step4_hours: `Emails the ${this.t('partner_owner')}.`,
+      };
+      return LADDER_RUNGS.map(rung => ({
+        ...rung,
+        hint: hints[rung.key] || rung.hint,
+      }));
+    },
+  },
   async mounted() {
     await this.load();
   },
@@ -224,15 +256,16 @@ export default {
       }
     },
     inFlightLabel(state) {
+      const partnerLower = this.t('partner', 'lower');
       return (
         {
           climbing: 'climbing',
-          dealer_replied: 'dealer replied — halted',
+          dealer_replied: `${partnerLower} replied — halted`,
           acknowledged_by_agent: 'marked acknowledged — halted',
           resolved: 'case resolved — halted',
-          no_dealer: 'no dealer — not climbed',
+          no_dealer: `no ${partnerLower} — not climbed`,
           no_step_one: 'never escalated by EM-7',
-          dealer_lookup_failed: 'dealer lookup failed',
+          dealer_lookup_failed: `${partnerLower} lookup failed`,
           ladder_complete: 'ladder complete',
         }[state] || state
       );
@@ -327,7 +360,9 @@ export default {
       // A dealer configured purely by role is valid -- that is the shape the
       // ladder wants -- so either half satisfies "name somebody".
       if (!this.dealerForm.dealer || (!emails.length && !Object.keys(contacts).length)) {
-        useAlert('Dealer and at least one member email or named contact are required.');
+        useAlert(
+          `${this.t('partner')} and at least one member email or named contact are required.`
+        );
         return;
       }
       this.savingDealer = true;
@@ -337,11 +372,14 @@ export default {
           contacts,
           region: this.dealerForm.region,
         });
-        useAlert('Dealer saved.');
+        useAlert(`${this.t('partner')} saved.`);
         this.dealerForm = { ...EMPTY_DEALER_FORM };
         await this.load();
       } catch (err) {
-        useAlert('Failed to save dealer: ' + (err.message || String(err)));
+        useAlert(
+          `Failed to save ${this.t('partner', 'lower')}: ` +
+            (err.message || String(err))
+        );
       } finally {
         this.savingDealer = false;
       }
@@ -380,28 +418,38 @@ export default {
           contacts,
           region: this.editDealerForm.region,
         });
-        useAlert('Dealer updated.');
+        useAlert(`${this.t('partner')} updated.`);
         this.cancelEditDealer();
         await this.load();
       } catch (err) {
-        useAlert('Failed to update dealer: ' + (err.message || String(err)));
+        useAlert(
+          `Failed to update ${this.t('partner', 'lower')}: ` +
+            (err.message || String(err))
+        );
       } finally {
         this.savingDealer = false;
       }
     },
     async removeDealer(dealer) {
       // eslint-disable-next-line no-alert
-      if (!window.confirm(`Delete dealer "${dealer}"? This cannot be undone.`)) {
+      if (
+        !window.confirm(
+          `Delete ${this.t('partner', 'lower')} "${dealer}"? This cannot be undone.`
+        )
+      ) {
         return;
       }
       this.savingDealer = true;
       try {
         await deleteDealer(dealer);
-        useAlert('Dealer deleted.');
+        useAlert(`${this.t('partner')} deleted.`);
         if (this.editingDealerName === dealer) this.cancelEditDealer();
         await this.load();
       } catch (err) {
-        useAlert('Failed to delete dealer: ' + (err.message || String(err)));
+        useAlert(
+          `Failed to delete ${this.t('partner', 'lower')}: ` +
+            (err.message || String(err))
+        );
       } finally {
         this.savingDealer = false;
       }
@@ -417,8 +465,8 @@ export default {
     </h1>
     <p class="mb-6 text-sm text-n-slate-11">
       Configure which person-in-charge (PIC) is notified for each
-      department, and which email address represents each dealer, for SLA
-      and escalation notifications.
+      department, and which email address represents each
+      {{ t('partner', 'lower') }}, for SLA and escalation notifications.
     </p>
 
     <div v-if="loading" class="py-10 text-sm text-center text-n-slate-11">
@@ -466,9 +514,9 @@ export default {
           v-if="ladderForm.enabled && !ladderForm.dry_run"
           class="p-2 mb-4 text-xs rounded text-n-slate-12 bg-n-amber-3"
         >
-          Reminders will be emailed to real dealer contacts. Run with dry run
-          on first and read the log for a full working week before switching
-          it off.
+          Reminders will be emailed to real {{ t('partner', 'lower') }}
+          contacts. Run with dry run on first and read the log for a full
+          working week before switching it off.
         </p>
 
         <div class="grid grid-cols-1 gap-3 md:grid-cols-2">
@@ -528,7 +576,9 @@ export default {
           <thead>
             <tr class="text-left border-b border-n-weak text-n-slate-12">
               <th class="px-3 py-2 font-medium whitespace-nowrap">Case</th>
-              <th class="px-3 py-2 font-medium whitespace-nowrap">Dealer</th>
+              <th class="px-3 py-2 font-medium whitespace-nowrap">
+                {{ t('partner') }}
+              </th>
               <th class="px-3 py-2 font-medium whitespace-nowrap">Rung</th>
               <th class="px-3 py-2 font-medium whitespace-nowrap">Next step</th>
               <th class="px-3 py-2 font-medium whitespace-nowrap">Due in</th>
@@ -768,7 +818,9 @@ export default {
       </div>
 
       <!-- Dealer section -->
-      <h2 class="mb-3 text-base font-medium text-n-slate-12">Dealer groups</h2>
+      <h2 class="mb-3 text-base font-medium text-n-slate-12">
+        {{ t('partner') }} groups
+      </h2>
       <form
         class="grid max-w-2xl grid-cols-1 gap-4 mb-6 md:grid-cols-2"
         @submit.prevent="createDealer"
@@ -836,13 +888,13 @@ export default {
             :disabled="savingDealer"
             class="px-4 py-2 text-sm font-medium rounded-lg w-fit bg-n-brand text-white hover:opacity-90 disabled:opacity-50"
           >
-            Add dealer
+            Add {{ t('partner', 'lower') }}
           </button>
         </div>
       </form>
 
       <p v-if="!dealers.length" class="text-sm text-n-slate-11">
-        No dealers configured yet.
+        No {{ t('partner', 'plural_lower') }} configured yet.
       </p>
       <div v-else class="overflow-x-auto border rounded-lg border-n-weak">
         <table class="w-full text-sm border-collapse text-n-slate-11">
diff --git a/app/javascript/dashboard/views/ProtonRsaPage.vue b/app/javascript/dashboard/views/ProtonRsaPage.vue
index 633dcea..dcc1b83 100644
--- a/app/javascript/dashboard/views/ProtonRsaPage.vue
+++ b/app/javascript/dashboard/views/ProtonRsaPage.vue
@@ -8,6 +8,7 @@
      useAlert toasts). Field names match Task 20's _IncidentRequest exactly. -->
 <script>
 import { useAlert } from 'dashboard/composables';
+import { useCustomFeatures } from 'dashboard/composables/useCustomFeatures';
 import {
   listRsaIncidents,
   getRsaAggregate,
@@ -43,6 +44,10 @@ function toLocalInput(value) {
 
 export default {
   name: 'ProtonRsaPage',
+  setup() {
+    const { t } = useCustomFeatures();
+    return { t };
+  },
   data() {
     return {
       aggregate: { by_cause: [], by_dealer: [] },
@@ -87,7 +92,8 @@ export default {
       } catch (err) {
         this.errored = true;
         useAlert(
-          'Failed to load RSA incidents: ' + (err.message || String(err))
+          `Failed to load ${this.t('field_incident', 'plural_lower')}: ` +
+            (err.message || String(err))
         );
       } finally {
         this.loading = false;
@@ -97,11 +103,14 @@ export default {
       this.saving = true;
       try {
         await createRsaIncident(this.buildPayload(this.form));
-        useAlert('Incident logged.');
+        useAlert(`${this.t('field_incident')} logged.`);
         this.form = { ...EMPTY_FORM };
         await this.load();
       } catch (err) {
-        useAlert('Failed to log incident: ' + (err.message || String(err)));
+        useAlert(
+          `Failed to log ${this.t('field_incident', 'lower')}: ` +
+            (err.message || String(err))
+        );
       } finally {
         this.saving = false;
       }
@@ -128,28 +137,38 @@ export default {
       this.saving = true;
       try {
         await updateRsaIncident(id, this.buildPayload(this.editForm));
-        useAlert('Incident updated.');
+        useAlert(`${this.t('field_incident')} updated.`);
         this.cancelEdit();
         await this.load();
       } catch (err) {
-        useAlert('Failed to update incident: ' + (err.message || String(err)));
+        useAlert(
+          `Failed to update ${this.t('field_incident', 'lower')}: ` +
+            (err.message || String(err))
+        );
       } finally {
         this.saving = false;
       }
     },
     async remove(id) {
       // eslint-disable-next-line no-alert
-      if (!window.confirm('Delete this incident? This cannot be undone.')) {
+      if (
+        !window.confirm(
+          `Delete this ${this.t('field_incident', 'lower')}? This cannot be undone.`
+        )
+      ) {
         return;
       }
       this.saving = true;
       try {
         await deleteRsaIncident(id);
-        useAlert('Incident deleted.');
+        useAlert(`${this.t('field_incident')} deleted.`);
         if (this.editingId === id) this.cancelEdit();
         await this.load();
       } catch (err) {
-        useAlert('Failed to delete incident: ' + (err.message || String(err)));
+        useAlert(
+          `Failed to delete ${this.t('field_incident', 'lower')}: ` +
+            (err.message || String(err))
+        );
       } finally {
         this.saving = false;
       }
@@ -160,18 +179,20 @@ export default {
 
 <template>
   <div class="w-full h-full p-6 overflow-y-auto">
-    <h1 class="mb-1 text-lg font-medium text-n-slate-12">RSA Incident Log</h1>
+    <h1 class="mb-1 text-lg font-medium text-n-slate-12">
+      {{ t('field_incident') }} Log
+    </h1>
     <p class="mb-6 text-sm text-n-slate-11">
-      Log roadside-assistance incidents and review a running summary by cause
-      and dealer. Manual staff data entry — this does not connect to any
-      dispatch system.
+      Log {{ t('field_incident', 'plural_lower') }} and review a
+      running summary by cause and {{ t('partner', 'lower') }}. Manual staff
+      data entry — this does not connect to any dispatch system.
     </p>
 
     <div v-if="loading" class="py-10 text-sm text-center text-n-slate-11">
       Loading…
     </div>
     <div v-else-if="errored" class="py-10 text-sm text-center text-n-slate-11">
-      Could not load RSA incidents.
+      Could not load {{ t('field_incident', 'plural_lower') }}.
     </div>
 
     <template v-else>
@@ -182,7 +203,7 @@ export default {
             Cases by cause
           </h2>
           <p v-if="!aggregate.by_cause.length" class="text-sm text-n-slate-11">
-            No incidents yet.
+            No {{ t('field_incident', 'plural_lower') }} yet.
           </p>
           <ul v-else class="flex flex-col gap-1 m-0 list-none">
             <li
@@ -197,10 +218,10 @@ export default {
         </div>
         <div class="p-4 border rounded-lg border-n-weak bg-n-alpha-black2">
           <h2 class="mb-3 text-sm font-medium text-n-slate-12">
-            Cases by dealer
+            Cases by {{ t('partner', 'lower') }}
           </h2>
           <p v-if="!aggregate.by_dealer.length" class="text-sm text-n-slate-11">
-            No incidents yet.
+            No {{ t('field_incident', 'plural_lower') }} yet.
           </p>
           <ul v-else class="flex flex-col gap-1 m-0 list-none">
             <li
@@ -217,7 +238,7 @@ export default {
 
       <!-- (b) Data-entry form -->
       <h2 class="mb-3 text-base font-medium text-n-slate-12">
-        Log an incident
+        Log {{ t('field_incident', 'lower') }}
       </h2>
       <form
         class="grid max-w-3xl grid-cols-1 gap-4 mb-10 md:grid-cols-2"
@@ -225,7 +246,7 @@ export default {
       >
         <div>
           <label class="block mb-1 text-sm text-n-slate-11"
-            >Incident date *</label
+            >{{ t('field_incident') }} date *</label
           >
           <input
             v-model="form.incident_date"
@@ -236,7 +257,7 @@ export default {
         </div>
         <div>
           <label class="block mb-1 text-sm text-n-slate-11"
-            >Vehicle no. *</label
+            >{{ t('asset_id') }} *</label
           >
           <input
             v-model="form.vehicle_no"
@@ -247,7 +268,7 @@ export default {
         </div>
         <div>
           <label class="block mb-1 text-sm text-n-slate-11"
-            >Vehicle model</label
+            >{{ t('asset_model') }}</label
           >
           <input
             v-model="form.vehicle_model"
@@ -365,15 +386,17 @@ export default {
             :disabled="saving"
             class="px-4 py-2 text-sm font-medium rounded-lg w-fit bg-n-brand text-white hover:opacity-90 disabled:opacity-50"
           >
-            Log incident
+            Log {{ t('field_incident', 'lower') }}
           </button>
         </div>
       </form>
 
       <!-- (c) Incident table with inline edit / delete -->
-      <h2 class="mb-3 text-base font-medium text-n-slate-12">Incidents</h2>
+      <h2 class="mb-3 text-base font-medium text-n-slate-12">
+        {{ t('field_incident', 'plural') }}
+      </h2>
       <p v-if="!incidents.length" class="text-sm text-n-slate-11">
-        No incidents logged yet.
+        No {{ t('field_incident', 'plural_lower') }} logged yet.
       </p>
       <div v-else class="overflow-x-auto border rounded-lg border-n-weak">
         <table class="w-full text-sm border-collapse text-n-slate-11">
@@ -381,9 +404,11 @@ export default {
             <tr class="text-left border-b border-n-weak text-n-slate-12">
               <th class="px-3 py-2 font-medium whitespace-nowrap">Date</th>
               <th class="px-3 py-2 font-medium whitespace-nowrap">
-                Vehicle no.
+                {{ t('asset_id') }}
+              </th>
+              <th class="px-3 py-2 font-medium whitespace-nowrap">
+                {{ t('asset_model') }}
               </th>
-              <th class="px-3 py-2 font-medium whitespace-nowrap">Model</th>
               <th class="px-3 py-2 font-medium whitespace-nowrap">Cause</th>
               <th class="px-3 py-2 font-medium whitespace-nowrap">
                 Purchased from
