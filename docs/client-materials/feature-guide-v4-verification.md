# Feature Guide v4 — verification ledger (internal)

**Not a client deliverable.** Companion to `feature-guide-v3-pending.md`.

v3 was cut back on 2026-08-09 because it described software that was not
running. This file exists so v4's claims can be audited rather than trusted:
one row per checkable claim, naming the probe that settled it.

## Live state, measured 2026-08-11

| Probe | Value |
|---|---|
| `docker exec proton-chatwoot-rails cat /app/.git_sha` | `0866fda` |
| Chatwoot image | `asia-southeast1-docker.pkg.dev/lv-playground-genai/proton-images/proton-chatwoot:v4.15.1-custom-rc6` |
| Rendered `/app/login` feature list | `ai_assist,nav_menu,copilot,knowledge,inbound_alerts,faq_suggestion_popup` |
| Backend `/openapi.json` path count | 113 |
| BigQuery views (P8) | Not "could not be probed" — a dataset named `demo_proton` **does** exist in project `lv-playground-genai` (the same project the Chatwoot image is hosted in) with **8 views**, not the 11 named in the guide: `v_bounce_rate`, `v_csat`, `v_fallback_rate`, `v_nps`, `v_quality`, `v_resolution_split`, `v_speed_of_response`, `v_volume_by_month_channel` — plus 3 base tables (`conversations`, `qa_labels`, `turn_events`). The `demo_` prefix on the dataset name means it is **not confirmed to be the production dataset** the guide's P8 claim refers to; Task 11 should treat this as "partially exists, under a name that looks like a demo/staging artifact" rather than either "created" or "not created," and decide the AI Cost & Performance section's fate accordingly. |

Full capture: see the `## Raw capture` section below.

## Sanctioned probes

1. **What the SPA ships** — the feature list from the rendered login page,
   read inside the container. Not the compose file, not the patch directory.
2. **What the backend serves** — `/openapi.json` listed **by prefix**. An
   exact-path check is banned: `/alerts/rules` reads as missing when the real
   paths are `/alerts/rules/{defaults,mine}`.
3. **Flag state** — `printenv` inside *both* `proton-agent` and
   `proton-chatwoot-rails`. They do not read the same source: the backend
   takes the tenant env file wholesale via `env_file:`, Rails gets only what
   the compose `x-chatwoot-env` block passes through, and the VM's compose
   file has been stale before.
4. **The browser** — for anything only a rendered page can settle.

## Ledger

| Chapter | Claim | Probe | Verdict | Date |
|---|---|---|---|---|
| 01 | Brings WhatsApp, email, and phone/IVR conversations into a single inbox | raw capture: agent/backend env `WHATSAPP_MEDIA_UNDERSTANDING_ENABLED`, `PHONE_LANGUAGE_NUDGE_ENABLED`; openapi prefixes `/voice`, `/webhooks` | verified | 2026-08-12 |
| 01 | AI assistant "that can draft or send replies" | raw capture: agent/backend env `AGENT_MODE=auto` | verified | 2026-08-12 |
| 01 | Roadside-assistance (RSA) incident logging is part of the platform | raw capture: agent/backend env `RSA_ENABLED=true`; openapi prefix `/rsa` | verified | 2026-08-12 |
| 01 | Vehicle/service lookup (Customer 360) is part of the platform | raw capture: openapi prefix `/admin/customer360` | verified | 2026-08-12 |
| 01 | Case tracking is part of the platform | raw capture: openapi prefix `/cases` | verified | 2026-08-12 |
| 01 | Reporting built around dealer and PIC escalation | raw capture: openapi prefixes `/metrics/dealer-escalation`, `/admin/escalation` | verified | 2026-08-12 |
| 01 | Admin-only pages Integrations, Escalation Routing, SLA Policies, Audit Log, Roles & Permissions sit in the sidebar | raw capture: openapi prefixes `/admin/integrations/dms`, `/admin/escalation`, `/admin/sla-policy`, `/admin/audit`, `/authz` | verified | 2026-08-12 |
| 01 | AI-assist buttons "Ask Copilot, Suggest a reply, Summarize" above the reply box | raw capture: openapi prefixes `/assist/copilot`, `/assist/suggest`, `/assist/summarize`; SPA feature list includes `copilot` | verified | 2026-08-12 |
| 01 | Administrators can be granted fine-grained permissions under Administration → Roles & Permissions | raw capture: backend env `RBAC_ENABLED=true`; openapi prefix `/authz/roles`; source `deploy/chatwoot-fork/patches/0059-roles-permissions-redesign.patch` | verified | 2026-08-12 |
| 01 | "As an agent, you see Conversations, Contacts, and Knowledge (read access) ... As an administrator, you additionally see Cases, the RSA Incident Log, Customer 360, Reports, and Administration" | source: `deploy/chatwoot-fork/patches/0009-knowledge-nav.patch` gives the Knowledge route `permissions: ['administrator', 'agent']` with no read-only gate on any editing control anywhere in patches 0009-0022, and `backend/apps/backend/src/chatbot/features/authz/seed.py` registers a `knowledge.edit` permission but no router enforces it — so Knowledge is not read-only for agents. Cases *is* correctly admin-only: `deploy/chatwoot-fork/patches/0043-cases-list.patch` gates `proton/cases` at `permissions: ['administrator']`; RSA is the same via `deploy/chatwoot-fork/patches/0035-rsa-incident-log.patch` | corrected | 2026-08-12 |
| 01 | Login screen offers a "Forgot password?" reset-email link | not settled by raw capture (only the SPA's compiled feature-flag list was captured, not the login page markup); would need a live browser check | unverifiable | 2026-08-12 |
| 01 | Interface language can be switched between English and Indonesian from the profile menu | not settled by raw capture (no locale env var or SPA evidence captured); would need a live browser check of the profile settings menu | unverifiable | 2026-08-12 |
| 01 | Sidebar includes a reachable "Campaigns/Help Center" entry | not settled by raw capture — no env/openapi/SPA-feature-list evidence for stock Chatwoot Campaigns/Help Center visibility on this tenant (see chapter 08 rows) | unverifiable | 2026-08-12 |
| 03 | Contacts area is a single cross-channel directory (WhatsApp, email, phone/IVR) | raw capture: same channel evidence as chapter 01's channel-unification row | verified | 2026-08-12 |
| 03 | Contacts from inbound WhatsApp/email/phone messages are added automatically, no manual step | raw capture: channels confirmed active; core Chatwoot contact-creation behavior | verified | 2026-08-12 |
| 03 | Contact profile shows custom attributes such as vehicle model | source: `backend/apps/backend/src/chatbot/features/chat/customer360_router.py` reads `custom_attributes.vehicle_model` off live Chatwoot conversations | verified | 2026-08-12 |
| 03 | Customer 360's conversation list is the same data the contact profile shows, "so the two views never disagree" | source: `customer360_router.py` calls the same `ChatwootAdapter.list_contact_conversations` / `list_conversations` methods the profile view reads through | verified | 2026-08-12 |
| 03 | Customer 360 is visible only to administrators granted the Customer 360 permission | source: `deploy/chatwoot-fork/patches/0041-customer360-admin.patch` gates the sidebar entry on `protonHasPermission('customer360.view')`; backend gates the route on `require_permission("customer360.view")` | verified | 2026-08-12 |
| 03 | Customer 360 search requires "at least two characters" | source: backend `Query(min_length=2)` and frontend `if (q.length < 2)` check, both in `customer360_router.py` / patch 0041 | verified | 2026-08-12 |
| 03 | Phone search is an exact match; vehicle-number search matches any conversation whose vehicle model contains the value | source: `customer360_router.py` — `_pick_best_contact` (digits-only exact match) vs. substring match against `vehicle_model` / `vehicle_no` | verified | 2026-08-12 |
| 03 | Customer 360 is read-only — "nothing here can be created, edited, or deleted" | source: `customer360_router.py` exposes only `GET /admin/customer360/search`; patch 0041 comment states "no create/update/delete here" | verified | 2026-08-12 |
| 03 | DMS/TSP block shows a "Not connected" notice when unreachable and a "Mock data" notice when the client is mocked | source: `deploy/chatwoot-fork/patches/0045-dms-integration-card.patch` adds exactly these two notices to `ProtonCustomer360Page.vue`, gated on `dms.status !== 'ok'` / `dms.mock` | verified | 2026-08-12 |
| 03 | Notes and segments exist as contact-level features | not settled by raw capture — stock Chatwoot contact features, no Proton env flag or backend route gates them; would need a live browser check | unverifiable | 2026-08-12 |
| 08 | Campaigns (one-off + ongoing) menu is present/reachable in the sidebar on this tenant | not settled by raw capture — Campaigns is a stock Rails feature with no Proton env flag or backend route; would need a live browser check with an administrator account | unverifiable | 2026-08-12 |
| 08 | Help Center portal menu is present/reachable (sidebar or account settings) on this tenant | not settled by raw capture; `deploy/chatwoot-fork/patches/0059-roles-permissions-redesign.patch` surfaces the native `knowledge_base_manage` permission ("Manage Help Center portals and articles"), confirming the capability is registered in the build but not that a portal is configured/visible for this tenant | unverifiable | 2026-08-12 |
| 08 | Knowledge → FAQs/Documents/Assistants is a separate feature from the Help Center portal | raw capture: openapi prefixes `/kb/faq`, `/kb/documents`, `/kb/assistants`; env `KNOWLEDGE_PG_ENABLED=true` | verified | 2026-08-12 |
| 08 | Campaigns send through whichever inbox/channel they're attached to, no separate setup | not settled by raw capture (depends on stock Rails Campaigns being reachable at all — see the reachability row above) | unverifiable | 2026-08-12 |
| 02 | The conversation list brings in WhatsApp, the website chatbot, email, and phone/IVR alike | raw capture: agent/backend env `WHATSAPP_MEDIA_UNDERSTANDING_ENABLED`, `PHONE_LANGUAGE_NUDGE_ENABLED`; openapi prefixes `/voice`, `/webhooks` (same evidence as chapter 01's channel-unification row) | verified | 2026-08-12 |
| 02 | Administrators can set which channel each agent handles first, steering automatic assignment (Administration → Inboxes) | raw capture: backend env `ROUTING_ENABLED=true`; openapi prefixes `/routing/priorities`, `/routing/assign` | verified | 2026-08-12 |
| 02 | The **escalate** label triggers the two-thread escalation email workflow | raw capture: agent/backend env `EMAIL_ESCALATION_ENABLED=true`, `ESCALATION_EMAIL_ENABLED=true`; openapi prefix `/escalation/notify` | verified | 2026-08-12 |
| 02 | The automated escalation email only fires on an Email-channel conversation; applying `escalate` on WhatsApp/web chatbot/phone changes only the label | source: `agent/app/services/dept_suggestion.py` and `sync.maybe_escalate` both gate on `inbox.channel_type == "Channel::Email"`; raw capture confirms `ESCALATION_ALL_CHANNELS` is unset on both containers (per Task 3's finding) | verified | 2026-08-12 |
| 02 | A dealer label routes to a dealer **group** — every member is forwarded the case, not just one address | source: `backend/apps/backend/src/chatbot/features/chat/pic_store.py`'s `DealerRecord.emails: list[str]` (a list, not a single address) | verified | 2026-08-12 |
| 02 | "All six departments now route somewhere" — `dept_sales/engineer/pre_sales/aftersales/cs/technical` each have a PIC configured, and both `dealer_komang_motor`/`dealer_caroline_motor` do too | not settled by raw capture — no PIC/dealer directory contents were captured (only route paths). A stale docstring in `agent/app/services/dept_suggestion.py` states "`dept_aftersales`, `dept_cs`, `dept_technical` on the Proton tenant today" had **no** PIC at time of writing, which conflicts with this claim; the PIC/dealer stores are Firestore-backed and editable at runtime, so this may since have changed. Fix round 1: reviewer attempted `GET /admin/escalation/pics`/`/dealers` from inside the container and both returned HTTP 401 (RBAC-gated, needs a user token, not an API key) — still not settled. **Prose amended** to stop asserting the coverage count: it now states the routing mechanism (a department/dealer with no contact configured sends the escalation email to nobody) and tells the operator to check the Escalation Routing page, without claiming how many are populated. Would need the browser sweep's Escalation Routing admin-page pass (with a user session) to settle the actual count | unverifiable | 2026-08-12 |
| 02 | AI-suggested escalation department: live on this tenant, off by default at the code level | raw capture: agent/backend env `DEPT_SUGGESTION_ENABLED=true` (code default is `False` per `agent/app/config.py`) | verified | 2026-08-12 |
| 02 | AI-suggested escalation department only fires on Email-channel conversations, posts once per conversation, and only ever proposes a department that has a PIC configured | source: `agent/app/services/dept_suggestion.py` — `channel_type != "Channel::Email"` gate, `dept_suggested_at` first-write-wins stamp, and candidates sourced only from the PIC store (`GET /escalation/departments`), never a static list | verified | 2026-08-12 |
| 02 | Dealer/PIC reply is linked back as a private note `Reply from <name> <email>:`, and a second reply from the same address doesn't post another note | source: `agent/app/services/escalation_replies.py` — exact f-string match, and `existing.get(_REPLIED_ATTR)` early-return before the internal-reply path | verified | 2026-08-12 |
| 02 | Customer's own reply can't post inline on this tenant's Email inbox (`{"error":"Incoming messages are only allowed in Api inboxes"}`), so it lands as a private note prefixed `Customer's own reply (from <email>, could not be posted inline -- see conversation <id>):`, and the conversation still reopens | source: `agent/app/services/escalation_replies.py` — exact error text and f-string match in the module docstring and body; a customer, unlike a dealer/PIC, is unstamped and may reply more than once | verified | 2026-08-12 |
| 02 | The conversation picks up an `escalation_replied` label, and a second private note titled `Suggested customer reply (draft — review before sending):` appears beneath a dealer/PIC reply | source: `agent/app/services/escalation_replies.py` — `_REPLIED_LABEL = "escalation_replied"`; exact f-string match for the AI-drafted reply note, gated on `settings.escalation_reply_draft_enabled` (raw capture: `ESCALATION_REPLY_DRAFT_ENABLED=true`) | verified | 2026-08-12 |
| 02 | SLA breach: a private note starting `⚠️ SLA breach` is posted, and the department's PIC group is separately emailed, with a Tier-2 re-alert | source: `backend/apps/backend/src/chatbot/features/chat/sla.py` — exact `f"⚠️ SLA breach ({to_state}) on case {ticket_id}..."` and `f"⚠️ TIER-2 escalation..."` strings; raw capture: `SLA_ALERT_EMAIL_ENABLED=true`, `SLA_ALERT_NOTE_ENABLED=true`, `SLA_ENGINE_ENABLED=true` | verified | 2026-08-12 |
| 02 | "Today this only scans the Email inbox" (SLA breach alerts) | raw capture: agent/backend env `SLA_INBOX_IDS=4` — a single inbox id configured | verified | 2026-08-12 |
| 02 | The contact side panel now opens by default, rather than needing manual expansion each time | source: `deploy/chatwoot-fork/patches/0004-contact-panel-default.patch` — `isContactSidebarOpen === undefined ? true : isContactSidebarOpen` | verified | 2026-08-12 |
| 02 | Conversation summaries are written in English regardless of the conversation's original language | source: `backend/apps/backend/src/chatbot/features/assist/router.py` `_SUMMARIZE_SYSTEM` — "Reply in English regardless of the conversation language." | verified | 2026-08-12 |
| 02 | Inactivity timers (idle warning, then automatic close after a grace period) are configurable per inbox | raw capture: agent/backend env `LIFECYCLE_ENABLED=true`; openapi prefix `/kb/inboxes/{inbox_id}/timing`; source `backend/apps/backend/src/chatbot/features/chat/adapters/inbox_timing_store.py` stores `idle_warn_minutes`/`idle_close_grace_minutes`/`idle_close_out_of_hours_grace_minutes` per inbox_id | verified | 2026-08-12 |
| 02 | A conversation resolved via inactivity auto-close (or by hand) may trigger a satisfaction-survey message [fix round 1: this row's original "corrected" text — "does not currently send one" — was itself wrong; see the two rows below, which supersede it] | raw capture: agent/backend env `LIFECYCLE_SURVEY_ENABLED=false`; source `agent/app/services/lifecycle.py` `on_human_resolved` returns early when the flag is off | corrected | 2026-08-12 |
| 02 | Resolving a conversation sends the customer Chatwoot's own native CSAT satisfaction-rating request, on all four live inboxes | live read-only query of `Inbox.csat_survey_enabled`, 2026-08-12: `[1] Proton API (Channel::Api)=true, [2] Website Demo (Channel::WebWidget)=true, [3] Twilio Proton (Channel::TwilioSms)=true, [4] Email (Channel::Email)=true` — all four `true` | verified | 2026-08-12 |
| 02 | The AI assistant's own lifecycle rating prompt ("rate our AI assistant/support agent from 1 to 5", distinct from native CSAT) is not currently switched on for this tenant | raw capture: agent/backend env `LIFECYCLE_SURVEY_ENABLED=false`; source `agent/app/services/lifecycle.py` — `on_human_resolved` and the bot's own end-of-chat survey step both gate on this flag | verified | 2026-08-12 |
| 02 | AI auto-draft: **suggest mode** is described as "the default" tenant-wide | raw capture: agent/backend env `AGENT_MODE=auto` (code default in `agent/app/config.py` is `agent_mode: str = "suggest"`, but this tenant overrides it); `agent/app/services/orchestrator.py` — `effective_mode = per_inbox_mode or settings.agent_mode` | corrected | 2026-08-12 |
| 02 | Translate composer action: renders and works | raw capture: SPA feature list includes `ai_assist` (gates button visibility, per `deploy/chatwoot-fork/patches/0055-translate-action.patch`); openapi path `/assist/translate` present (backend route exists) — both halves of the probe pass | verified | 2026-08-12 |
| 02 | FAQ suggestion strip: renders and works | raw capture: SPA feature list includes `faq_suggestion_popup`; openapi path `/kb/suggest` present; agent/backend env `FAQ_SUGGESTION_POPUP_ENABLED=true` — both halves of the probe pass | verified | 2026-08-12 |
| 02 | Inbound alerts (toast on new customer message): renders and fires | raw capture: SPA feature list includes `inbound_alerts`; agent/backend env `INBOUND_ALERTS_ENABLED=true`; source `deploy/chatwoot-fork/patches/0057-inbound-alerts.patch` — `raiseAlert(...)` is called only for the `new_inbound` event | verified | 2026-08-12 |
| 02 | Inbound alerts: per-agent alert-preferences page (sound/desktop notification, other event types) is a separate surface and is NOT enabled on this tenant | raw capture: `ALERT_RULES_ENABLED` is absent from both the agent and backend printed env (code default in `backend/apps/backend/src/chatbot/platform/config.py` is `alert_rules_enabled: bool = False`); source `backend/apps/backend/src/chatbot/features/alerts/rules_router.py` — every endpoint answers `{"disabled": true}` when the flag is off, and the fork's own test suite documents the built-in default for `new_inbound` as toast-only | verified | 2026-08-12 |
| 02 | Multimodal AI assist: "Suggest a reply" reads image/video/audio attachments, and media-derived terms lead the knowledge-base search when a conversation has media | raw capture: agent/backend env `ASSIST_MEDIA_UNDERSTANDING_ENABLED=true`, `WHATSAPP_MEDIA_UNDERSTANDING_ENABLED=true`; source `backend/apps/backend/src/chatbot/features/assist/assist_media.py` (`collect_media_parts`) and `backend/apps/backend/src/chatbot/features/assist/router.py` (`_kb_context` puts media-derived terms at the front of the retrieval query) | verified | 2026-08-12 |
| 02 | By default the conversation list shows every conversation across all statuses on sign-in | not settled by raw capture (default UI landing state, not env/route observable); would need a live browser check | unverifiable | 2026-08-12 |
| 04 | Knowledge routes exist for FAQs, Documents/Uploads, Assistants, Scenarios, Playground, Tools, Inboxes (assignment), and Settings | raw capture: openapi prefixes `/kb/faq`, `/kb/faq/bulk`, `/kb/documents`, `/kb/knowledge`, `/kb/knowledge/file`, `/kb/knowledge/text`, `/kb/assistants`, `/kb/scenarios`, `/kb/tools`, `/kb/tools/builtins/{name}`, `/kb/inboxes`, `/kb/inboxes/{inbox_id}`, `/kb/inboxes/{inbox_id}/timing`, `/kb/settings`; env `KNOWLEDGE_PG_ENABLED=true` | verified | 2026-08-12 |
| 04 | FAQ bulk upload accepts a UTF-8 CSV with `question`/`answer` (required) and `;`-separated `keywords`/`tags`, reports created count and per-row skip reasons, and imported entries are always saved active | source: `backend/apps/backend/src/chatbot/features/chat/faq_admin_router.py::bulk_create_faq` — exact column names, `;`-split, `active=True` always on the bulk path | verified | 2026-08-12 |
| 04 | FAQ entries and the Documents corpus are also the source for the FAQ suggestion strip agents see above the reply box (chapter 2), not just Suggest-a-reply/Copilot | raw capture: openapi `/kb/suggest`; SPA feature list includes `faq_suggestion_popup`; source `backend/apps/backend/src/chatbot/features/chat/kb_suggest_router.py` — merges live-FAQ hits (ranked first) with the Vertex/Documents corpus into one `suggestions` list | verified | 2026-08-12 |
| 04 | Playground behaves like the Ask Copilot panel because it calls the same backend endpoint | source: `deploy/chatwoot-fork/patches/0014-knowledge-playground.patch` — Playground's request function posts to `/assist/copilot` | verified | 2026-08-12 |
| 04 | Custom tools are capped at 15 per tenant | source: `backend/apps/backend/src/chatbot/features/chat/kb_tools_router.py` — "Hard cap: ≤ 15 custom tools per tenant (409 if exceeded)" | verified | 2026-08-12 |
| 04 | Each assistant can hold up to 20 scenarios; instruction text is capped at 4,096 characters | source: `backend/apps/backend/src/chatbot/features/chat/kb_scenarios_router.py` — `_INSTRUCTION_MAX = 4096`, "≤ 20 scenarios per assistant — 409 if the cap would be breached on create" | verified | 2026-08-12 |
| 04 | Inbox assignment mode is one of Off / Suggest / Auto, validated server-side | source: `backend/apps/backend/src/chatbot/features/chat/kb_inboxes_router.py` — `_VALID_MODES = frozenset({"off", "suggest", "auto"})`, 422 on anything else | verified | 2026-08-12 |
| 04 | The Settings page's 9 assistant-persona message fields (Welcome, Handoff, Idle warning, Idle close, Resolution prompt, Survey AI, Survey agent, Thanks, Assign agent) plus the unused Resolution message match the backend's `AssistantConfig`, and the 4 Features toggles match its `feature_faq`/`feature_memory`/`feature_citations`/`feature_contact_attributes` fields | source: `backend/apps/backend/src/chatbot/features/chat/adapters/assistants_store.py::AssistantConfig` field list | verified | 2026-08-12 |
| 04 | Tenant settings panel exposes exactly 10 keys (Assist/Copilot Gemini model, Copilot max tool iterations, AI assist/Copilot/AI drafts enabled, Default mode, Debounce seconds, Inbound auto-ack template, Escalation ack template) | source: `backend/apps/backend/src/chatbot/features/chat/settings_facade.py::_ALL_KEYS` — "All ten managed keys" | verified | 2026-08-12 |
| 04 | Welcome/disclaimer message on conversation start does not fire on this tenant today | raw capture: agent/backend env `LIFECYCLE_DISCLAIMER_ENABLED=false`; source `agent/app/services/lifecycle.py::on_conversation_created` — non-Email branch returns before calling `_welcome_text` when the flag is off (Email branch is independently gated on `EMAIL_AUTOACK_ENABLED`, also `false` in raw capture) | verified | 2026-08-12 |
| 04 | The assistant's own Survey AI / Survey agent / Thanks messages do not fire on this tenant today — a conversation resolved by the bot or by a human does not trigger this page's rating request | raw capture: agent/backend env `LIFECYCLE_SURVEY_ENABLED=false`; source `agent/app/services/lifecycle.py` — `handle_lifecycle_reply`'s survey branch and `on_human_resolved` both return early when the flag is off | verified | 2026-08-12 |
| 04 | The idle warning, automatic close, and "is your case resolved?" prompt ARE live on this tenant, and the Assign agent message fires from that same flow regardless of the survey flag | raw capture: agent/backend env `LIFECYCLE_ENABLED=true`; source `agent/app/services/lifecycle.py::handle_lifecycle_reply` — the `AWAITING_RESOLUTION`/"not resolved" branch posts `assign_agent` unconditionally, only the "resolved" branch's survey step is gated on `LIFECYCLE_SURVEY_ENABLED` | verified | 2026-08-12 |
| 04 | Customers still get a satisfaction-rating request on resolve, from Chatwoot's own native CSAT survey — a separate mechanism from the two rows above, configured per inbox rather than per assistant, and not exposed on this Settings page at all | live read-only query of `Inbox.csat_survey_enabled`, 2026-08-12 (see the `### native CSAT and greeting per inbox` raw-capture subsection): `true` on all four inboxes | verified | 2026-08-12 |
| 04 | Per-inbox idle-timer minutes, a per-inbox on/off switch for the idle-warning/auto-close step, and per-inbox overrides of the same message wording live on that inbox's own settings page under Administration → Inboxes, not on Knowledge → Inboxes or Knowledge → Settings, and win over the assistant persona message when both are set | source: `deploy/chatwoot-fork/patches/0023-inbox-inactivity-timing.patch` adds this panel to the native inbox settings page (`WeeklyAvailability.vue`), calling `GET/PUT /kb/inboxes/{inbox_id}/timing`; `agent/app/services/lifecycle.py::_resolve_lifecycle_message` — "Per-inbox override -> persona message -> SOP default" | verified | 2026-08-12 |
| 04 | Chatwoot's native per-inbox greeting message is ON for the Twilio Proton (WhatsApp) and Email inboxes, OFF for Proton API and Website Demo — a mechanism distinct from both the assistant's own Welcome message (off tenant-wide) and native CSAT | [fix round 1: supersedes this row's original "unverifiable" verdict — the brief's premise that the data was already captured did not hold at the time, and Task 7 correctly declined to fabricate a value; the coordinator has since supplied the live query's full output] live read-only query `Inbox.all.each { |i| puts "...csat_survey_enabled=#{i.csat_survey_enabled} | greeting_enabled=#{i.greeting_enabled}" }`, run against the live tenant 2026-08-12 (see the `### native CSAT and greeting per inbox` raw-capture subsection): `greeting_enabled=false` on Proton API and Website Demo, `greeting_enabled=true` on Twilio Proton and Email. The query shows on/off state only, not the configured greeting text | verified | 2026-08-12 |
| 05 | Cases list is a filterable table view of conversations — no separate case record, unassigned/uncategorized conversations show a dash | source: `deploy/chatwoot-fork/patches/0043-cases-list.patch` — reads Chatwoot's own `/conversations` endpoint, no new backend storage; null fields render as `—` by convention | verified | 2026-08-12 |
| 05 | Cases page requires the same permission as Customer 360 | source: `0043-cases-list.patch` — "gated on the existing customer360.view permission (there's no backend require_permission route to match, since this reads Chatwoot directly)" | verified | 2026-08-12 |
| 05 | Case list columns: Case ID, Division, Concern, Purchased From, Escalated To, Agent, Car Plate, Aging (days), Status | source: `0043-cases-list.patch` (all but Agent) + `0048-assignee-visible.patch` ("Adds an Agent column to the custom Cases list table ... between Aging (days) and Status") | verified | 2026-08-12 |
| 05 | Case list filters: Division, Case type, Status, Channel, Dealer, plus Reset filters | source: `0043-cases-list.patch` docstring — "Filters: division, case type, status, channel, dealer" | verified | 2026-08-12 |
| 05 | Agent column mirrors the conversation's own assignee; is a Cases-list-only addition not (yet) on the conversation card in the regular inbox view | source: `0048-assignee-visible.patch` — reads `conversation.meta.assignee.name`, and explicitly states the matching `ConversationCard.vue` change was deferred/not included | verified | 2026-08-12 |
| 05 | A banner appears when the list is capped, meaning filters/totals no longer reflect every case | source: `0043-cases-list.patch` — `MAX_PAGES = 40` × 25/page = 1,000-conversation cap; `fetchAllCases()` returns a `truncated` flag the page renders as an amber Banner | verified | 2026-08-12 |
| 05 | Five case-categorisation custom attributes exist, all single-select dropdowns on the conversation, with exactly 3 / 8 / 89 / 246 / 4 values for Case Type / Case Category / Case Subcategory / Case Detail / Vehicle Model | raw capture: `### live case-taxonomy custom attribute definitions` — live `CustomAttributeDefinition` query, all five `model=conversation_attribute display=list`, values 3/8/89/246/4 exactly matching the guide's figures | verified | 2026-08-12 |
| 05 | Only Case Category → Case Subcategory → Case Detail cascade; Case Type and Vehicle Model are independent and don't affect any other field; changing a value clears stale children below it in the chain | source: `deploy/chatwoot-fork/patches/0036-case-category-hierarchy.patch` + `0050-case-detail-hierarchy.patch` — `CASCADE_ATTRS = ['case_category','case_subcategory','case_detail']` only; `onUpdate()` walks every level below the changed one, clearing a child whose value no longer starts with `<parent>: ` | verified | 2026-08-12 |
| 05 | Case Subcategory/Case Detail values are prefixed with their full parent path (`<Division>: <Level 1>`, `<Division>: <Level 1>: <Level 2>`), with Level 3/4 folded into Case Detail via an em dash | source: `0050-case-detail-hierarchy.patch` — prefix-filter logic + commit message ("Level 3/4 folded into each value using an em dash", citing `agent/app/config.py`'s `CASE_DETAIL_OPTIONS_JSON` convention) | verified | 2026-08-12 |
| 05 | **New in this edition:** Case Category, Case Subcategory, and Case Detail are administered from a dedicated Case Taxonomy admin page (not the general Custom Attributes settings page), reachable only to administrators with a dedicated permission; edits sync into the live dropdown immediately, no restart; values are retired, never deleted | source: `deploy/chatwoot-fork/patches/0060-taxonomy-admin.patch` (`ProtonTaxonomyPage.vue`, sidebar entry + `proton/taxonomy` route gated on `taxonomy.manage`, admin-only per `authz/seed.py`) + `0062`/`0063` (dialog editors); backend `features/taxonomy/router.py` (`/admin/taxonomy/tree`,`/node`,`/node/{key}/retire`) + `features/taxonomy/chatwoot_sync.py` (`sync_taxonomy_to_chatwoot` PATCHes Chatwoot's `case_category`/`case_subcategory`/`case_detail` custom-attribute definitions on every save/retire); raw capture: backend env `TAXONOMY_ADMIN_ENABLED=true`; openapi confirms all three routes mounted. Caveat recorded here, not in the chapter: patch 0060's own commit message states this was "never applied to a real Chatwoot checkout... nothing here has been seen rendering in a browser" — verified from source/flag state per this project's established convention for admin pages (see chapter 01/03 rows), not from a live render | verified | 2026-08-12 |
| 05 | **Correction, supersedes the prior edition's instruction:** don't edit Case Category/Subcategory/Detail values from the general Custom Attributes settings page anymore — a value added there can be silently overwritten | source: `features/taxonomy/chatwoot_sync.py` module docstring — "The store is authoritative, the Chatwoot sync is downstream" — and `sync_taxonomy_to_chatwoot()` rebuilds the *full* option list for all three attributes from the taxonomy store on every `create_node`/`retire_node` call (`router.py`), so any value the store doesn't know about is dropped the next time anyone saves/retires a node via the admin page | verified | 2026-08-12 |
| 05 | **[fix round 1] Correction:** the Case Taxonomy page's four tiers, labelled Type/Division/Category/Detail, do not map to the conversation's dropdowns tier-for-tier — the page's **Type** tier is what feeds the conversation's **Case Category** dropdown, not its own **Division** tier; **Division**+**Category** together feed **Case Subcategory**; **Detail** feeds **Case Detail**; the conversation's separate **Case Type** field (Inquiry/Complaint/Compliment & Feedback) is untouched by this page entirely, despite the name overlap with the page's Type tier | source: `features/taxonomy/store.py` module docstring — "Provides a 4-level taxonomy (Type -> Division -> Category -> Detail)" — and `ProtonTaxonomyPage.vue`'s own `LEVEL_LABELS = {1: 'Type', 2: 'Division', 3: 'Category', 4: 'Detail'}` (patch `0060-taxonomy-admin.patch`), cross-checked against `features/taxonomy/chatwoot_sync.py::sync_taxonomy_to_chatwoot`, which syncs `level==1` labels to `case_category`, `level==3` labels (prefixed by their `level==2` parent's label) to `case_subcategory`, and `level==4` labels to `case_detail` — `level==2` is never synced to a dropdown of its own, only used as the prefix text folded into Case Subcategory's values. Independently re-verified by the reviewer against the store docstring and live Vue page copy before this fix round | verified | 2026-08-12 |
| 05 | Case Type and Vehicle Model are not part of the Case Taxonomy admin page/tree and have no dedicated admin page of their own; still provisioned as flat option lists under Custom Attributes | source: `features/taxonomy/chatwoot_sync.py::sync_taxonomy_to_chatwoot` only ever calls `sync_custom_attribute` for `case_category`/`case_subcategory`/`case_detail` — `case_type` and `vehicle_model` never appear | verified | 2026-08-12 |
| 05 | Case Category, Case Subcategory, Case Type, and Vehicle Model are set automatically by the AI assistant; Case Detail is never set by the AI | source: `agent/app/services/categorize.py::maybe_categorize` (resolution-time fallback — writes `case_category`/`case_subcategory`/`case_type`/`vehicle_model`, never `case_detail`) and `backend/apps/backend/src/chatbot/features/chat/agents.py::classify_ticket_tool` (mid-conversation, same four fields, never `case_detail`), gated respectively by `lifecycle_auto_categorize` (agent fallback, off on this tenant per raw capture) and `CHAT_AGENT_ENABLED=true` (mid-conversation tool, on). The taxonomy content feeding the mid-conversation tool (`case_taxonomy_json`/option lists) was not itself captured, so whether it is currently non-empty on this tenant is not independently confirmed — noted here rather than in the chapter | verified | 2026-08-12 |
| 05 | These five categorisation values feed the Cases list and the Departments & PIC / Case Lifecycle reports | source: `backend/apps/backend/src/chatbot/features/metrics/mapping.py` reads `case_category`/`case_subcategory`/`case_detail`/`vehicle_model`/derived `division` off conversation custom attributes into the same row shape the metrics/reports pipeline consumes | verified | 2026-08-12 |
| 06 | RSA Incident Log is a standalone manual-entry page; logging an incident does not dispatch a tow truck or notify anyone | source: `backend/apps/backend/src/chatbot/main.py` mounts `build_rsa_router` only `if settings.rsa_enabled and settings.rsa_database_url`; raw capture: `RSA_ENABLED=true` both containers; openapi confirms `/rsa/incidents*` present; router has no dispatch/notification side effect, pure CRUD | verified | 2026-08-12 |
| 06 | RSA Incident Log shares its sidebar visibility with the SLA Policies page — same permission | source: `deploy/chatwoot-fork/patches/0035-rsa-incident-log.patch` — comment states RSA has no dedicated `rsa.manage` permission and is deliberately gated on `sla.manage`, "the same operational-admin cohort that manages SLA policies" | verified | 2026-08-12 |
| 06 | Cases by cause / Cases by dealer summary at the top of the page, updating immediately after a log/edit | source: `backend/apps/backend/src/chatbot/features/rsa/rsa_repository.py::aggregate()` returns `by_cause`/`by_dealer` counts; `/rsa/incidents/aggregate` route recomputes from current rows on every call | verified | 2026-08-12 |
| 06 | Required fields Incident date/Vehicle no./Cause; optional fields Vehicle model, Purchased from, Breakdown location, Arrived location, Customer called-in time, Towing assigned time, Time arrived breakdown area, Time arrived outlet, Total km, Late reason, Remarks | source: `backend/apps/backend/src/chatbot/features/rsa/rsa_router.py::_IncidentRequest` — exact field list, `incident_date`/`vehicle_no`/`cause` required (no default), rest `| None = None` | verified | 2026-08-12 |
| 06 | Edit/Delete per row; Delete cannot be undone | source: `rsa_router.py` — `PATCH /rsa/incidents/{id}` (edit) and `DELETE /rsa/incidents/{id}` (hard delete, no soft-delete/undo field in `_IncidentRequest`/repository) | verified | 2026-08-12 |
| 06 | Customer 360's vehicle-number search matches RSA incidents on the Vehicle no. field, case-insensitive substring (partial plate works) | source: `backend/apps/backend/src/chatbot/features/chat/customer360_router.py::search` — `needle in (row.vehicle_no or "").lower()` on the non-phone branch | verified | 2026-08-12 |
| 06 | **Correction:** RSA incident data does NOT feed the Reports chapter's Departments & PIC or Case Lifecycle reports — it surfaces only in Customer 360 and this page's own Cases by cause/dealer summary | codebase-wide search: `rsa_repo`/`RsaRepository`/`rsa_incidents` are consumed only by `rsa_router.py` (this page) and `customer360_router.py` (Customer 360); no file under `features/metrics/` references RSA at all — the prior draft's claim that RSA data "feeds Proton's wider reporting" via those two named reports was unsupported | corrected | 2026-08-12 |
| 07 | `/metrics/*` route inventory: `/metrics/targets` confirmed absent; present under the prefix are `/metrics/after-hours`, `/metrics/ai-cost`, `/metrics/anomalies`(`/hourly`), `/metrics/by-tag`, `/metrics/callcenter`, `/metrics/case-aging`(`/export`), `/metrics/control-items`, `/metrics/dashboard`, `/metrics/dealer-escalation`(`/export`), `/metrics/departments`(`/export`), `/metrics/export`, `/metrics/freshness`, `/metrics/lifecycle`, `/metrics/sla-buckets`(`/export`), `/metrics/volume-by-type`(`/export`) | raw capture: `### backend openapi paths`, full `/metrics/*` prefix scan | verified | 2026-08-12 |
| 07 | Standard reports' three Proton sections exist exactly as described: Agent → **Tasks per Agent (Proton)**; Bot → **Proton AI & Channel Analytics**; CSAT → **Cross-Channel CSAT & NPS** | source: `deploy/chatwoot-fork/patches/0020-reports-native-merge.patch` — `ProtonAgentsSection.vue`, `ProtonBotSection.vue`, `ProtonCsatSection.vue`, wired into `AgentReportsIndex.vue`/`BotReports.vue`/`CsatResponses.vue`; each calls `getCallCentre()`/`getDashboard()` against `/metrics/callcenter`/`/metrics/dashboard`, both present in openapi | verified | 2026-08-12 |
| 07 | Bot report's "bot-resolved %" / "bot-vs-agent resolution split" do not measure AI vs. human — derived from the case's status alone | source: `ProtonBotSection.vue` sums `closed_by_bot`/`transfer_to_agent` from `v_resolution_split`; `backend/apps/backend/src/chatbot/features/metrics/mapping.py::_resolved_by` — `"agent" if status in {"open","pending","hold"}` (or, for the Chatwoot path, `{"open","pending","snoozed"}`) `else "bot"` — any closed/resolved case counts as "bot", whether a human or the bot closed it; no message-level or escalation-level signal is consulted. A more honest `v_ai_resolution`/`v_ai_vs_human` view exists in `bigquery_schema.py` but is not wired into this report | verified | 2026-08-12 |
| 07 | NPS tiles are empty; the end-of-conversation NPS survey is not running on this platform, so a score can only reach the tile via a manual `POST /chat/nps` | source: `backend/apps/backend/src/chatbot/platform/config.py` — `nps_sample_rate: float = 0.0`, comment: "0.0 (default) = no NPS question is ever asked, exactly as today"; raw capture confirms `NPS_SAMPLE_RATE` is absent from both containers' env (default applies). **Caveat noted here, not in the chapter:** a proactive NPS-send code path does exist (`backend/apps/backend/src/chatbot/features/chat/router.py`'s Zendesk-handback-triggered survey, gated by `should_survey_nps`/`nps_sample_rate`), but it is (a) inert at the default 0.0 rate and (b) part of the vendored backend's own Zendesk-handback flow, not the Chatwoot webhook flow (`agent/app/services/lifecycle.py`, `LIFECYCLE_SURVEY_ENABLED=false`) this CRM actually runs on. Either way, no NPS question goes out today | verified | 2026-08-12 |
| 07 | Anomaly, Departments & PIC, and Case Lifecycle report pages are reachable at **Reports → Anomaly / Departments & PIC / Case Lifecycle**, with the exact tables/columns/thresholds described (Flagged Channels count + channel/volume/baseline/deviation table with yellow ≥2σ / red ≥3σ badges; Department/PIC Performance + Top Departments by Cases + Reopen/CRR + Category × Vehicle Model; Resolution-Time Distribution with 0–30m/…/>24h buckets + Case State Trend + Recent Cases + Work-in-Progress/Case Aging) | source: `deploy/chatwoot-fork/patches/0020-reports-native-merge.patch` (`ProtonAnomaly.vue`, `ProtonDepartments.vue`, `ProtonCaseLifecycle.vue`, `Sidebar.vue` labels "Anomaly"/"Departments & PIC"/"Case Lifecycle", `reports.routes.js` routes `proton_anomaly_reports`/`proton_departments_reports`/`proton_case_lifecycle_reports`) + `0034-reporting-extensions.patch` (adds Category × Vehicle Model, `ProtonWipAgingSection` on Case Lifecycle) | verified | 2026-08-12 |
| 07 | The three new report routes are gated on `administrator` **or** `report_manage` permission, not administrator-only | source: `reports.routes.js` diff in `0020-reports-native-merge.patch`/`0044-weekly-report.patch` — `meta: { permissions: ['administrator', 'report_manage'] }` on all four Proton report routes | verified | 2026-08-12 |
| 07 | Weekly Report page/route, its week picker (defaults to current Monday–Sunday, start date freely choosable, e.g. Friday-start), and its section list (Case Volume, Case Status Trend, Inquiry/Complaint/Feedback Detail — Departments & PIC, Call Centre & SLA Performance, Work-in-Progress/Case Aging, Dealer Escalation Turnaround, Per-Case Detail) each carrying a "This week"/"All time" scope badge | source: `deploy/chatwoot-fork/patches/0044-weekly-report.patch` — `ProtonWeeklyReport.vue` (route `proton_weekly_report`, sidebar label "Weekly Report"); exact section headings matched verbatim; `SCOPE_STATUS.UNFILTERED`/period-scoped badges per section per the file's own header comment | verified | 2026-08-12 |
| 07 | SLA reports page: native SLA table + Proton's Cross-Channel SLA Achievement + SLA Compliance by Bucket (by case type) | source: `0020-reports-native-merge.patch` (`SLAReports.vue`, `ProtonSlaSection.vue` — "Cross-Channel SLA Achievement") + `0034-reporting-extensions.patch` (`ProtonSlaComplianceSection.vue` — "SLA Compliance by Bucket") | verified | 2026-08-12 |
| 07 | Dealer Escalation Turnaround table (cases escalated, avg/P50/P90 turnaround in days, Slowest cases sub-table) appears on both the SLA reports page and the Weekly Report, same underlying numbers; the turnaround clock starts once, the first time a dealer label is applied, and is not retroactive to case creation | source: `0034-reporting-extensions.patch`'s `ProtonDealerEscalationSection.vue` (reused unmodified by `0044`'s Weekly Report); `agent/app/services/sync.py::maybe_stamp_dealer_escalation` — idempotent, first-write-wins `dealer_escalated_at` custom-attribute stamp on the first `dealer_<slug>` label | verified | 2026-08-12 |
| 07 | **Central finding — corrects "there is nothing to turn on" and every "no separate setup" claim about the Proton report sections:** every Proton-built report section listed above (Standard reports' three Proton sections, Anomaly, Departments & PIC, Case Lifecycle, Weekly Report's warehouse-fed sections, SLA reports' two Proton sections, Dealer Escalation Turnaround's table) reads from a BigQuery connection that defaults OFF and was never switched on for this tenant, so today they render fixed illustrative numbers, not this account's real data, with no on-page indicator that can tell an operator so | source: `backend/apps/backend/src/chatbot/platform/config.py` — `metrics_provider: Literal["noop","bigquery"] = "noop"`; raw capture confirms `METRICS_PROVIDER`/`BIGQUERY_*` are absent from both containers' full env dump (defaults apply); `backend/apps/backend/src/chatbot/features/metrics/query_adapter.py::build_metrics_query_port` — `if settings.metrics_provider == "bigquery": ... else: return MockMetricsQuery()`, wired as the single shared `query_port` for every `/metrics/*` route in `main.py` (no per-route provider check). `MockMetricsQuery` (`query_port.py`) returns fixed, plausible-looking canned rows (e.g. a hard-coded WhatsApp/web anomaly pair), not empty data and not a "briefly unreachable" failure. **Correction (fix round 1): the claim that no scope/status signal ever reaches the frontend was itself wrong and has been corrected here.** `scopes` *is* surfaced on some responses — `period_query.py::wrap_period_response` (called from `insights_router.py` whenever a period is supplied) includes a `scopes` map pairing each block's current/previous `BlockScope` — and it *is* read on the client: patch `0044`'s `ProtonWeeklyReport.vue` binds `volumeScopeStatus`/`stateTrendScopeStatus` to `scopes.volume.current.status` / `scopes.state_trend.current.status` off the `/metrics/volume-by-type` and `/metrics/lifecycle` responses, driving the real `ProtonScopeBadge` shown on those two sections. The reason this indicator still can't distinguish mock from real is narrower and more precise than "no signal exists": `MockMetricsQuery`'s own docstring states that in the deliberate (non-degraded) mode "every block reports 'unfiltered': they are all-time figures, honestly labelled as such" (`self._scope = _UNFILTERED_SCOPE`, `query_port.py` line 672) — and "unfiltered" is the exact same status a genuinely-connected block that is all-time *by design* legitimately carries (this is why the five methods with no period support at all — `fetch_departments`/`fetch_callcenter`/`fetch_dealer_escalation`/`fetch_sla_buckets`/`fetch_case_aging` — have their badges hardcoded to `SCOPE_STATUS.UNFILTERED` as a prop in `ProtonDepartments.vue`/`ProtonWeeklyReport.vue` rather than read from any response field, since those responses carry no `scopes` channel to read in the first place). So the indicator exists and is genuinely read where it exists; it simply cannot discriminate "canned by design" from "genuinely unfiltered," because both are honestly the same label. A `grep` for "mock"/"demo"/"sample" across all three reporting patches (`0020`, `0034`, `0044`) still returns zero hits — there is no separate, dedicated mock/demo flag anywhere in the response shape or the frontend | corrected | 2026-08-12 |
| 07 | `demo_proton`/`lv-playground-genai` (Task 3's BigQuery probe finding) is not an unrelated demo artifact — it is the literal **default** value of `bigquery_project_id`/`bigquery_dataset`, and `deploy/tenants/example.env` warns explicitly: "a real tenant must override them or its metrics land in someone else's dataset" | source: `backend/apps/backend/src/chatbot/platform/config.py` lines 298-299; `deploy/tenants/example.env`'s "Metrics warehouse" comment block | verified | 2026-08-12 |
| 07 | The `demo_proton` dataset's 8 confirmed views map 1:1 to the 8 fields `/metrics/dashboard` returns (resolution/csat/nps/speed/fallback/bounce/quality/volume) — i.e. even if the connection were switched on today, only the Bot/CSAT report's Proton sections have their views present; Departments & PIC, Case Lifecycle, Weekly Report's warehouse sections, SLA's Proton sections, and Dealer Escalation Turnaround all reference additional views (`v_dept_pic_performance`, `v_reopen_rate`, `v_category_by_vehicle_model`, `v_state_trend`, `v_tasks_per_agent`, `v_sla_achievement`, `v_case_lifecycle`, `v_dealer_escalation_slowest_cases`, `v_case_aging`, etc.) that do not exist in that dataset — each would degrade to an empty/"no data" state (never a 500) per `query_adapter.py::_query_block`'s catch-all | source: `backend/apps/backend/src/chatbot/features/metrics/query_adapter.py` — view names referenced by `_fetch_dashboard_sync`/`_fetch_departments_sync`/`_fetch_callcenter_sync`/`_fetch_case_lifecycle`/`_fetch_dealer_escalation`/etc., cross-checked against the raw capture's `bq ls demo_proton` listing | verified | 2026-08-12 |
| 07 | `/metrics/ai-cost` and `/metrics/control-items` exist as backend routes but are read by no Chatwoot fork patch/Vue component — no operator-reachable page surfaces them; consistent with `AI Cost & Performance Measurement` already being correctly held back to `feature-guide-v3-pending.md` (per `feature-guide-src-v4/OUTLINE.md`) rather than described in this chapter | codebase-wide `grep` across `deploy/chatwoot-fork/patches/*.patch` for `ai-cost`/`aiCost`/`control-items`/`controlItems`: zero hits | verified | 2026-08-12 |
| 07 | Exact set and order of native report tabs (Overview/Conversation/CSAT/Agent/Label/Inbox/Bot) as rendered in the sidebar | not settled by raw capture — reconstructing the final Sidebar.vue order requires tracing ~10 patches that each touch the Reports sidebar block sequentially; would need a live browser check of the rendered sidebar | unverifiable | 2026-08-12 |
| 07 | Exact on-page wording of the SLA reports page's headings/labels | not settled by raw capture — Vue template text was read from patch source, which matches what the chapter states, but exact live rendering (spacing, capitalisation as rendered) needs a live browser check | unverifiable | 2026-08-12 |
| 10 | `AGENT_MODE=auto` is the tenant-wide default mode, not Suggest — a customer's default experience is a reply that reaches them with no human review step | raw capture: agent/backend env `AGENT_MODE=auto`; `agent/app/services/orchestrator.py` `effective_mode = per_inbox_mode or settings.agent_mode` | verified | 2026-08-12 |
| 10 | **Correction:** the chapter's "Suggest mode vs. Auto mode" section previously framed Suggest as an equally-likely or default mode (example scenario put Proton's general WhatsApp inbox in Suggest mode), which contradicted chapter 02's corrected "auto mode, the tenant-wide default" claim. Rewritten to state Auto is the default and a reply reaches the customer unreviewed unless an inbox is explicitly overridden to Suggest | source: this file's own chapter-02 ledger row (`AI auto-draft: suggest mode is described as "the default"` — corrected) plus `feature-guide-src-v4/02-conversations.md`'s "AI auto-draft and suggest-vs-auto mode" section, which chapter 10 must not contradict | corrected | 2026-08-12 |
| 10 | `DEPT_SUGGESTION_ENABLED=true` — the AI-suggested escalation department note never applies the label itself, only ever suggests one in a private note, because an AI decision must not be able to trigger the escalation email on its own | raw capture: agent/backend env `DEPT_SUGGESTION_ENABLED=true`; source `agent/app/services/dept_suggestion.py` posts a note only, never calls the label-apply endpoint (same source cited by chapter 02's parallel row) | verified | 2026-08-12 |
| 10 | `ESCALATION_ALL_CHANNELS_ENABLED` is unset on this tenant — the `escalate` label only sends the two-thread escalation email on an Email-channel conversation; applying it on WhatsApp/web chatbot/phone changes only the label | raw capture: `ESCALATION_ALL_CHANNELS` absent from both the agent and backend full env dump (defaults apply); source `agent/app/services/sync.py::maybe_escalate` gates on `inbox.channel_type == "Channel::Email"` (same evidence as chapter 02's parallel row) | verified | 2026-08-12 |
| 10 | `LIFECYCLE_ENABLED=true`, `LIFECYCLE_DISCLAIMER_ENABLED=false`, `LIFECYCLE_SURVEY_ENABLED=false` — idle warning/auto-close/resolution prompt live, opening Welcome/disclaimer off, both AI-side satisfaction surveys off | raw capture: agent/backend env, all three flags; source `agent/app/services/lifecycle.py` (same evidence as chapter 02/04's parallel rows) | verified | 2026-08-12 |
| 10 | **Correction (the trap, caught again):** the chapter's lifecycle-messages note previously said the customer's opening acknowledgement on **Email only** comes from Chatwoot's own native greeting; the live query shows native greeting is also **on** for the **WhatsApp (Twilio Proton)** inbox, and off for the web chatbot and Proton API inboxes. Corrected to name both inboxes, matching chapter 04's already-corrected wording | source: `### native CSAT and greeting per inbox` raw capture, live query 2026-08-12 — `greeting_enabled=true` on Twilio Proton and Email, `false` on Proton API and Website Demo; cross-checked against `feature-guide-src-v4/04-knowledge.md`'s parallel corrected paragraph, which this chapter must not contradict | corrected | 2026-08-12 |
| 10 | **Removed:** the previous claim that the Email inbox's native greeting "goes to every new email thread — including a dealer replying to an escalation" | not settled by raw capture or by source — no probe evidence exists for greeting behaviour on a reply-triggered new conversation thread specifically (as opposed to a genuinely new customer email), and no other chapter makes this claim; removed rather than asserted, per the correction rule against inventing detail the ledger can't back. A live browser test replying to an active escalation email and watching for a duplicate greeting would settle it | removed | 2026-08-12 |
| 10 | **Correction:** "How to use it" previously stated all six department labels now have a PIC configured and route correctly, and that both `dealer_komang_motor`/`dealer_caroline_motor` are live dealer groups today — this repeats the same coverage claim chapter 02's ledger already walked back to unverifiable (PIC/dealer directory contents were never queried; a fix-round attempt to read `/admin/escalation/pics`/`/dealers` returned 401, RBAC-gated). Rewritten to match chapter 02's corrected wording: name the six department labels without asserting coverage, and tell the reader to check the Escalation Routing page instead | source: this file's chapter-02 ledger row on "All six departments now route somewhere" (unverifiable); `feature-guide-src-v4/02-conversations.md`'s Labels section ("Check the Escalation Routing page rather than assume every department is covered"), which this chapter must not contradict — note `feature-guide-src-v4/09-administration.md` still carries the old, uncorrected coverage claim, but that file is out of this task's scope | corrected | 2026-08-12 |
| 10 | Media-grounded retrieval — when a conversation has photo/video/voice-note media, terms drawn from that media (not just the customer's typed words) lead the knowledge-base search — applies not only to an agent's manual "Suggest a reply" click (chapter 02) but also to the assistant's fully automatic reply/handoff decision on WhatsApp | raw capture: agent/backend env `ASSIST_MEDIA_UNDERSTANDING_ENABLED=true`, `WHATSAPP_MEDIA_UNDERSTANDING_ENABLED=true`, `CHAT_AGENT_ENABLED=true`; source `agent/app/services/orchestrator.py` — `chat_agent_enabled` bypasses the legacy `gemini.decide` router entirely in favour of `_process_via_chat_agent`, which reads image/audio/video attachments off the trailing incoming messages (gated on `whatsapp_media_understanding_enabled`) and forwards them to the backend's `/chat/turn`; `backend/apps/backend/src/chatbot/features/chat/service.py::_media_kinds_in_user_content` feeds `has_image`/`has_video` into the same ADK agent turn that answers or hands off | verified | 2026-08-12 |
| 09 | **Held-back section 1 — Agent Availability & Workforce Dashboard: RESTORED.** Both surfaces (the Workforce dashboard and the agent status selector / My status page) are in the deployed SPA, and both backend routers are mounted | raw capture: Chatwoot `.git_sha=0866fda` = fork patch `0065`, so patches `0053`/`0054`/`0062`/`0065` are all in the image; openapi **prefix** scan finds `/admin/workforce`, `/routing/presence/status`, `/routing/presence/statuses`, `/routing/presence/statuses/{key}` (the v3 note's "`/routing/status` and `/routing/presence` absent" was an exact-path check against names that never existed — the banned probe); backend env `PRESENCE_TRACKING_ENABLED=true`, `PRESENCE_CUSTOM_STATUSES_ENABLED=true`; `main.py` mounts `build_workforce_router` under `presence_tracking_enabled` and `build_status_router` under `presence_custom_statuses_enabled`. **Answer to the brief's question: the routes were never missing code — the v3 probe was wrong, and the flags are now on.** Neither page is gated on the SPA `PROTON_FEATURES` list (patches `0053`/`0054` gate on `protonHasPermission('workforce.view')` / `('presence.set_own_status')`), so their absence from the rendered feature list is expected and not evidence against them | verified | 2026-08-12 |
| 09 | **Correction inside the restored section:** the 10-minute / 1-hour absence alerts do **not** fire on this tenant, and After-Call Work is **not** entered automatically at call end — the v3 draft described both as working | raw capture: `PRESENCE_THRESHOLD_ALERTS_ENABLED` and `ACW_ENABLED` are both absent from the agent and backend env dumps; `backend/apps/backend/src/chatbot/platform/config.py` lines 246-251 default both to `False`; `features/routing/presence_thresholds.py::sweep_presence_thresholds` returns `{"agents_checked": 0}` and does nothing when the flag is off. Operator-visible discrepancy recorded in the chapter: the My status page still renders an "(alerts)" marker beside every `counts_as_unavailable` status, so the page implies alerting that does not happen | corrected | 2026-08-12 |
| 09 | **Correction inside the restored section:** named statuses are chosen from the profile-menu availability control *as well as* the My status page — the v3 draft said the top-bar control "is left exactly as it is" and does not list named statuses | source: `deploy/chatwoot-fork/patches/0065-unify-availability-selector.patch` — `SidebarProfileMenuStatus.vue`'s `availabilityStatuses` returns the custom catalogue (minus `system_managed` entries) plus the native Offline entry whenever the catalogue is non-empty. Also recorded in the chapter: with custom statuses present none is marked active in that menu ("the catalogue does not report which one is set"), so the trigger falls back to the native Online/Busy/Offline label — the My status page is where the named status and its elapsed time are shown | corrected | 2026-08-12 |
| 09 | Eight named statuses (Available, Busy, Lunch, Break, Coaching, Training, Toilet, Prayer); Lunch/Break/Toilet/Prayer count as unavailable, Coaching/Training/Busy/Offline/After-Call-Work do not; statuses are never deleted, only retired by marking them not routable | source: `features/routing/custom_status.py::SEED_STATUSES` (exact keys/labels/`counts_as_unavailable` values) and its preceding comment block; `features/routing/status_router.py` module docstring — "**No delete.** The catalogue can be added to and edited, never deleted … `PUT` can retire a status by making it `routable=False`" | verified | 2026-08-12 |
| 09 | Workforce dashboard columns are Agent / Current status / Elapsed in status / Time in status today / Availability % (working day) / Open cases / Cases closed today / Availability history; it polls on a ~30s cadence rather than streaming | source: `0053-workforce-dashboard.patch` — the eight `<th>` strings verbatim, and `data.refresh?.recommended_interval_seconds || 30` with a reschedule (not `setInterval`); `features/routing/workforce_router.py` "Freshness, not a fake 'real-time' claim" section | verified | 2026-08-12 |
| 09 | "Cases closed today" is always blank, never 0; "Open cases" also blanks for every agent when the tally could not be established, rather than rendering the team as idle; availability % is measured against the inbox's working hours, not 24 hours | source: `features/routing/workforce_router.py` — `"cases_closed_today": None` unconditionally + `_CASES_CLOSED_TODAY_CAVEAT`; the three-state `open_case_count` logic + `open_case_count_caveat` (review finding I4); `_availability_percent` via `features.metrics.business_hours.working_minutes_between` keyed on the configured inbox, failing open to calendar minutes | verified | 2026-08-12 |
| 09 | Availability history is derived from transitions to and from the native Offline status only, and is not a login/logout record | source: `0053-workforce-dashboard.patch`'s own header ("deliberately titled 'Availability history', never 'Login/logout' -- it is derived from PresenceEvent transitions to/from Chatwoot's native `offline` status only, NOT a session record") | verified | 2026-08-12 |
| 09 | Three permissions split the area: `presence.set_own_status` (default Agent role), `workforce.view`, `workforce.manage` (administrator) | source: `backend/apps/backend/src/chatbot/features/authz/seed.py` — the three registry entries with those exact descriptions, and `presence.set_own_status` in the `agent` default grant list | verified | 2026-08-12 |
| 09 | **Held-back section 2 — AI Conversational Quality: RESTORED.** Both operator-visible surfaces render and their routes respond | raw capture: `.git_sha=0866fda` puts patches `0055-translate-action.patch` and `0056-faq-composer-apply.patch` in the image; openapi **prefix** scan finds `/assist/translate` and `/kb/suggest`; env `TRANSLATION_ENABLED=true`, `FAQ_SUGGESTION_POPUP_ENABLED=true`, and the rendered SPA feature list carries both `ai_assist` (gates the Translate button) and `faq_suggestion_popup` (gates the strip). Consistent with chapter 02's two already-verified rows for the same surfaces — chapter 09 cross-references rather than restating them | verified | 2026-08-12 |
| 09 | **Correction inside the restored section:** four of the section's behaviours are deployed but switched OFF on this tenant — sentiment classification, sentiment-matched tone, photo/video diagnosis, and both resolved-case settings (private-note summary on resolve, and the searchable resolved-case store). The FAQ keyword-overlap weight sits at its 0.0 default, which reproduces today's ranking exactly. The v3 draft's "Pending verification — these ship as unbuilt fork patches" framing is now wrong in the other direction and was removed | raw capture: `SENTIMENT_CLASSIFIER_ENABLED`, `SENTIMENT_TONE_ADJUSTMENT_ENABLED`, `MEDIA_DIAGNOSIS_PROMPT_ENABLED`, `RESOLVED_CASE_INDEX_ENABLED`, `AUTO_SUMMARY_ON_RESOLVE_ENABLED`, `FAQ_KEYWORD_WEIGHT` all absent from both containers' env dumps; `platform/config.py` lines 905-974 default them to `False`/`0.0` | corrected | 2026-08-12 |
| 09 | Photo/video diagnosis covers images and video only — voice notes are deliberately excluded; the tone adjustment ignores a sentiment older than ~15 minutes | source: `features/chat/prompts.py` line 133 — `if not media_diagnosis_prompt_enabled or not (has_image or has_video): return` (no audio branch); `features/chat/service.py` — `_TONE_SENTIMENT_TTL_SECONDS = 900.0` and `_sentiment_is_fresh` | verified | 2026-08-12 |
| 09 | Nothing surfaces resolved-case suggestions to an agent today, and the store's purge has no caller — emptying it means asking us to run a statement | codebase-wide grep: no non-test caller of `ResolvedCaseIndexer`'s `.purge()`; no suggestion/panel router (`kb_suggest_router.py`, `features/assist/router.py`) queries the resolved-case store — the only reference is a comment | verified | 2026-08-12 |
| 09 | Outbound Tamil replies to customers stay disabled pending a 30-enquiry evaluation scored by a Tamil speaker; inbound Tamil translation for the agent is what the live Translate action gives | source: `platform/config.py` lines 926-937 — `translation_outbound_tamil_enabled: bool = False` with the "do not flip this just because translation_enabled is on" comment; raw capture confirms the var is absent from both containers | verified | 2026-08-12 |
| 09 | The photo/video diagnosis prompt has still never been run against a real image sent through a real WhatsApp number | no probe available from source or from the container environment — this needs a real media message on a live WhatsApp number and a human reading the reply. Carried into the chapter as an explicit "not yet demonstrated" note plus a `<!-- VERIFY-LIVE -->` marker, and named in the pending file's header table as the one live check still owed for this section | unverifiable | 2026-08-12 |
| 09 | **Held-back section 3 — AI Cost & Performance Measurement: STAYS OUT.** Confirmed rather than assumed | raw capture: `/metrics/targets` absent from the full 113-path openapi list (prefix scan of `/metrics/*`, per chapter 07's inventory row). Chapter 07's central finding applies verbatim: `metrics_provider: Literal["noop","bigquery"] = "noop"` in `platform/config.py`, `METRICS_PROVIDER`/`BIGQUERY_*` absent from both containers, so `query_adapter.py::build_metrics_query_port` returns `MockMetricsQuery()` for every `/metrics/*` route — the AI cost and AI-performance numbers this section describes would be canned rows, not measurements. The only proton-named dataset is `demo_proton` in `lv-playground-genai`, which chapter 07 established is the *code default* for `bigquery_project_id`/`bigquery_dataset` rather than a provisioned tenant dataset, and holds 8 views, not the 11 the section names. `/metrics/ai-cost` and `/metrics/control-items` exist as routes but are read by no fork patch (chapter 07's zero-hit grep), so there is no operator-reachable page. All six P8 flags are absent from both containers' env. Section left in `feature-guide-v3-pending.md` with its `<!-- VERIFY-LIVE -->` comments intact | verified | 2026-08-12 |
| 09 | **Inherited correction (a):** chapter 09 no longer tells administrators to maintain Case Category / Case Subcategory / Case Detail under Custom Attributes — that page's instruction now points to the Case Taxonomy admin page and warns that its sync rewrites all three option lists in full on every save, so a value added under Custom Attributes can be silently overwritten. Case Type and Vehicle Model are explicitly kept under Custom Attributes | source: `features/taxonomy/chatwoot_sync.py::sync_taxonomy_to_chatwoot` (rebuilds the full option list for exactly those three attributes on every create/retire; `case_type`/`vehicle_model` never appear) — the same evidence behind chapter 05's two ledger rows. Chapter 09 cross-references chapter 05's four-tier naming-trap treatment rather than duplicating it, per the brief | corrected | 2026-08-12 |
| 09 | **Inherited correction (b):** chapter 09's "Escalation routing is now complete — all six department labels have a PIC configured and route correctly today, as do both live dealer groups" is removed. Replaced with the mechanism stated as fact (a department or dealer with no contact configured sends the escalation email to nobody, silently, with no error anywhere) plus an instruction to check the Escalation Routing page, matching chapters 02 and 10 | this file's chapter-02 row on "All six departments now route somewhere" (unverifiable; the authenticated `GET /admin/escalation/pics`/`/dealers` attempt returned HTTP 401, RBAC-gated) and chapter 10's parallel correction row, which explicitly flagged that `09-administration.md` still carried the uncorrected claim. Named dealer slugs `dealer_komang_motor`/`dealer_caroline_motor` also dropped from the chapter, since their existence was never probed either. Still needs the browser sweep's Escalation Routing pass with a user session to settle the actual coverage | corrected | 2026-08-12 |
| 09 | **Inherited correction (c):** the mock-metrics finding is now reflected in chapter 09 — the SLA Policies section no longer implies the SLA report pages measure this account's real performance against the targets set there. It states that the live breach-alert engine reads real conversations and applies a change immediately, while the reports chapter's Proton sections are illustrative until the reporting connection is switched on. The Audit Log section states it reads live records rather than the reporting warehouse, and the Workforce section says the same | this file's chapter-07 central-finding row (`metrics_provider` default `noop`, `MockMetricsQuery` for every `/metrics/*` route) cross-checked against `feature-guide-src-v4/07-reports.md`'s opening note, whose wording chapter 09 matches; `features/chat/sla.py` and `features/chat/audit_router.py` read live stores, not BigQuery | corrected | 2026-08-12 |
| 09 | **Inherited correction (d):** the native per-inbox greeting toggle and the native per-inbox satisfaction survey are now documented on chapter 09's Inboxes section, which owns that page, with their settled per-inbox state — greeting on for WhatsApp and Email, off for the API and website-demo inboxes; satisfaction survey on for all four. Per-inbox idle timers were already documented and are confirmed against source | raw capture `### native CSAT and greeting per inbox`, live query 2026-08-12: `csat_survey_enabled=true` on all four inboxes; `greeting_enabled=true` on Twilio Proton and Email, `false` on Proton API and Website Demo. Idle timers: `0023-inbox-inactivity-timing.patch` — the four field labels ("Warn after idle (min)", "Close grace — in business hours (min)", "Close grace — out of hours (min)", "Resolution-confirm grace (min)"), the `inactivity_enabled` toggle and the seven message-override fields all match the chapter verbatim | verified | 2026-08-12 |
| 09 | **The trap, checked:** three of the seven per-inbox message overrides (AI rating survey, Agent rating survey, Thank-you) belong to the assistant's own survey step, which is off tenant-wide — so wording entered there is stored but nothing sends it. Stated in the chapter, alongside the fact that the *native* per-inbox survey does send | raw capture: `LIFECYCLE_SURVEY_ENABLED=false`; source `agent/app/services/lifecycle.py` — `on_human_resolved` and the survey branch of `handle_lifecycle_reply` both return early when the flag is off, while the four remaining overrides (idle warning, chat closed, resolution prompt, assign-to-agent) run under `LIFECYCLE_ENABLED=true`. Consistent with chapters 02/04's rows; the chapter does **not** say customers get no survey, because native CSAT is on for all four inboxes | verified | 2026-08-12 |
| 09 | **New admin surface: the Agent Channel Priorities editor**, on an inbox's collaborators tab — per-agent Primary channel plus "Also handles" chosen from WhatsApp / Call / Email / Social / Web; the current primary's chip is disabled as an extra; Save appears only on a changed row, a saved row shows a tick and an untouched row a dash | source: `0024-agent-priorities.patch` (`AgentPrioritiesEditor.vue`, "Embedded in Settings → Inboxes → Collaborators tab", `CHANNELS = ['whatsapp','call','email','social','web']`) + `0063-agent-priorities-table.patch` (dirty-row Save, Saved tick/dash, primary chip disabled) + `0064-priorities-full-width.patch` (full-width layout); openapi `/routing/agents`, `/routing/priorities`, `/routing/priorities/{agent_id}`; env `ROUTING_ENABLED=true`. Consistent with chapter 02's already-verified row for the same feature | verified | 2026-08-12 |
| 09 | **Correction: the Roles & Permissions section described the pre-redesign page.** The deployed page has a role card rail (not a Role dropdown), a "+ New role" composer taking Name then an auto-slugged permanent Id (button "Create", not "Create role"), four conversation-visibility options including a "Default access" opt-out (the old three were renamed), a "Sections" sub-heading for the Contacts/Reports/Knowledge base toggles, seven named permission groups with per-group Grant all / Revoke all and a search box, a searchable people picker under "Members (N)" instead of a raw user-id + Assign box, and — the load-bearing change — a staged draft where nothing is written until "Save changes" | source: `deploy/chatwoot-fork/patches/0059-roles-permissions-redesign.patch`, which rewrites `ProtonRolesPermissionsPage.vue` wholesale; all labels quoted from its template. Permission groups verbatim: Knowledge & AI, Cases & taxonomy, Routing & escalation, Workforce, Alerts, Customer data, Administration (+ an "Other" catch-all that renders only if the backend returns an unmapped key) | corrected | 2026-08-12 |
| 09 | Roles & Permissions adds a self-lockout confirmation (revoking your own `roles.manage`) and a partial-save report that says how many changes applied and leaves the rest staged | source: `0059-roles-permissions-redesign.patch` — `lockoutPromptOpen`/`wouldLoseAccess` keyed on `LOCKOUT_KEY = 'roles.manage'`, and the amber `saveReport` banner ("Saved N of M changes." / "The remaining N change(s) … still staged below.") | verified | 2026-08-12 |
| 09 | **[fix round 1] Supersedes this row's original `removed` verdict.** A Chatwoot custom role **replaces** the base `administrator` permission array rather than adding to it, so enabling any "Chatwoot access" restriction (a Conversation visibility option other than Default access, or any Sections toggle) on a role whose members are administrators **strips their administrator rights** and locks them out of every Proton admin page and of Chatwoot Settings — including the Roles & Permissions page itself, so the affected people cannot undo it. Now warned about prominently in the chapter, in the box before step 2, plus a pointer from step 2 and a clarifying clause on the example scenario | **Provenance: operational, not source.** This is NOT derivable from anything in this checkout, and Task 11 originally and correctly declined to assert it — patch `0059` never mentions `administrator`, neither does `0028`, `features/authz/router.py::_resync_role_mirror` only PATCHes `custom_role_id` for members of a role whose `chatwoot.*` keys changed, and Chatwoot's enterprise `account_user.rb` (where the replacement actually happens) is upstream and not vendored here. The finding comes from **this project's own operations record of an observed, destructive incident on a live account**, supplied by the coordinator in fix round 1 and overruling the original omission. A later reader should treat it as field experience with more authority than the silent source, not as something they failed to find in the code — and should not "correct" the chapter by grepping for it. Source-side confirmation would need Chatwoot's upstream enterprise role model, or a live browser test assigning an administrator to a custom role carrying a `chatwoot.*` key and re-reading their access | corrected | 2026-08-12 |
| 09 | **[fix round 1] Correction:** the self-lockout confirmation was described as something "the page will stop you on", i.e. unconditional. It is not — it can silently fail to fire | source: `deploy/chatwoot-fork/patches/0059-roles-permissions-redesign.patch` — `wouldLoseAccess` is keyed on `currentUserId`, which is resolved by matching the signed-in user's email against the Chatwoot agent directory; that fetch is fail-open to `[]`, and with no resolved id `wouldLoseAccess` returns `false` and no modal is shown. The chapter now describes it as a check that catches the obvious case, names the email-matching dependency in client-facing terms ("has to work out which account is yours by matching your email address against the staff directory"), tells the operator to read back what they are about to save, and separates out the partial-save report, which does **not** depend on identifying the caller. A promised guard that might not engage is worse than no promise, especially in a security-relevant section | corrected | 2026-08-12 |
| 09 | **[fix round 1] Correction:** "seven permission groups" was stated as a fixed count; the page renders a group only when the backend returns at least one permission key belonging to it | source: `0059-roles-permissions-redesign.patch` — groups are built by bucketing the keys returned by the permission registry, so an empty bucket renders nothing; the live tenant's registry returns a subset of the full key set, so an administrator may see fewer than seven. Chapter now names the full set and says plainly that fewer may appear and why, and tells the reader to ask us about a missing group rather than work around it | corrected | 2026-08-12 |
| 09 | **[fix round 1] Handoff gap closed:** `10-ai-behaviour.md` still hard-coded `dealer_komang_motor`/`dealer_caroline_motor` in its escalation-label steps, contradicting the coverage correction applied to chapters 02, 09 and 10's own prose | the same evidence as this file's chapter-02 and chapter-09 coverage rows (dealer directory contents never probed; `/admin/escalation/dealers` returns 401 to an API key). Both slugs removed from that step, which now reads "the relevant dealer label if one is involved" and keeps its existing instruction to check the Escalation Routing page. A codebase grep confirms no named dealer slug remains anywhere in `feature-guide-src-v4/` except `11-scenarios.md`, which the coordinator is tracking under the next task and which was deliberately not touched here | corrected | 2026-08-12 |
| 09 | **[fix round 1]** The Escalation Routing section now carries a `<!-- VERIFY-LIVE -->` marker naming the open coverage question, so the browser sweep can find it the same way it will find the photo-diagnosis marker | this file's chapter-02 coverage row (unverifiable, 401 on the authenticated directory read). Marker names what to record — which of the six department rows carry a PIC email, and which dealer groups exist with at least one member — and states that no chapter may assert a coverage count until it is settled | verified | 2026-08-12 |
| 09 | DMS/TSP page: status badge values, the four configuration fields, the write-only masked Credential with its Replace button, the plain-text extra header, the 0.1–30s timeout, and Test connection reporting "Reachable" | source: `0045-dms-integration-card.patch` (`ProtonIntegrationsPage.vue`/`ProtonDmsIntegrationPage.vue`) + `features/chat/dms_admin_router.py`/`dms_config_store.py`/`dms_client.py`. Two corrections applied to the chapter: the badge has a fourth, transient "Checking status…" value and an "Enabled" badge carries a "reachability not yet verified" caption; and Test connection runs against the **saved** config, not the edited form. `public_dict()` omits the credential entirely, confirming it is never returned; `extra_header_value` is in `public_dict()`, confirming plain text. The 0.1 floor is browser-side only (the API accepts anything `> 0`, `<= 30`) — not stated in the chapter, since the operator only meets the browser bound | corrected | 2026-08-12 |
| 09 | SLA Policies page: Scope dropdown (Tenant default / a specific inbox), and the six fields Response window (hours) / Resolution window (hours) / Tier-2 re-alert after (hours) / Warn before breach (minutes) / Per-channel ACK minutes (JSON) / PIC WhatsApp number, with empty = inherit | source: `0025-sla-policies-admin.patch` + `0047-sla-policy-thresholds.patch` template labels verbatim (including the `{"whatsapp": 15}` placeholder); `features/chat/sla_policy_repository.py::resolve` merges per field, falling back to the tenant default then to deployed defaults in `features/chat/sla.py` | verified | 2026-08-12 |
| 09 | **Correction:** the SLA Policies section omitted the "Engine enabled override" checkbox, and its claim that a save "does not re-evaluate already-closed conversations" was imprecise | source: `0025-sla-policies-admin.patch` — `form.engine_enabled`, read by `features/chat/sla.py` (`if resolved_policy is not None and resolved_policy.engine_enabled is False`). On retroactivity: policy is resolved live per conversation on each scan, already-resolved conversations are excluded (`status != "resolved"` gate), and each rule is deduped against the audit trail, so a threshold change can neither re-fire nor erase a past breach — the chapter now says that instead. Also added: clearing a numeric field after typing in it can be rejected on save (`v-model.number` yields `''`, which the `float | None` body rejects with a 422), so "clear to inherit" is not reliably achievable from the form | corrected | 2026-08-12 |
| 09 | **Correction:** the Audit Log is not a log of case status changes only, and its column headers are shorter than the chapter stated | source: `0026-audit-log-admin.patch` — the five `<th>` values are exactly At / Ticket / Actor / Transition / Remark. Non-status writers appending to the same trail: `features/chat/sla.py` (breach markers, Tier-2), `features/chat/router.py` (first agent response), `features/chat/escalation_notifier.py` (escalation delivery outcome), `features/chat/escalation_router.py` (acknowledgement), `features/routing/router.py` (supervisor reassignment). Chapter rewritten to say so | corrected | 2026-08-12 |
| 09 | Audit Log filters: Actor is an exact match rather than a substring, the To date is inclusive of the whole picked day, and the list is capped at the most recent matches rather than the full history | source: `features/chat/adapters/audit_log.py` — `e.actor == actor` / Firestore `where("actor","==",actor)`; `0026-audit-log-admin.patch` sends `${this.toFilter}T23:59:59.999Z`; `features/chat/audit_router.py` — `limit: int = 200` applied after a newest-first sort. All three added to the chapter, since each one silently produces an empty or short result an operator would otherwise misread | verified | 2026-08-12 |
| 09 | The chapter intro's list of permission-gated left-hand-navigation pages was incomplete (it named five) | source: `backend/apps/backend/src/chatbot/features/authz/seed.py` permission registry + the sidebar gates in patches `0035`(RSA/`sla.manage`), `0039`(`escalation.manage`), `0041`(`customer360.view`), `0043`(Cases/`customer360.view`), `0045`(`integration.manage`), `0053`(`workforce.view`), `0054`(`presence.set_own_status`), `0060`(`taxonomy.manage`). Intro rewritten to name all of them and to call out My status as the deliberate agent-level exception | corrected | 2026-08-12 |
| 09 | Agents, Teams, Labels, Automation, Macros, Canned Responses and Account settings are stock Chatwoot settings pages, described from the native UI and carrying `<!-- VERIFY-LIVE -->` markers | not settled by raw capture — no Proton env flag, backend route or fork patch gates any of them, so nothing in the sanctioned probe set can confirm their exact on-screen wording; would need the live browser sweep. Left as written, markers intact | unverifiable | 2026-08-12 |
| 11 | **Known defect fixed.** Scenario 17 previously claimed the escalation-routing gap "is closed," that "all seven destinations… are now verified live," and that a supervisor "no longer need[s] to double-check" — retitled and rewritten to state the mechanism as fact (a department/dealer with no contact sends the escalation to nobody, silently, no error) and to send the reader to the Escalation Routing page without claiming a coverage count. `dealer_komang_motor`/`dealer_caroline_motor` removed | same evidence as this file's chapter-02/09/10 coverage rows (unverifiable; authenticated `GET /admin/escalation/pics`/`/dealers` returns HTTP 401). Wording matched against `02-conversations.md`'s Labels section and `09-administration.md`'s Escalation Routing section, which this chapter must not contradict | corrected | 2026-08-12 |
| 11 | Scenario 1 (WhatsApp inquiry to resolution) previously described the WhatsApp inbox "running in Suggest mode" with the assistant posting a private-note draft for an agent to review before sending | contradicts this file's chapter-02/10 rows (`AGENT_MODE=auto` is the tenant-wide default; a reply reaches the customer unreviewed unless an inbox is explicitly overridden). Rewritten: the inbox runs in Auto mode, the assistant sends its answer directly, and a human only picks the conversation up when the customer needs something arranged (a booking) that requires a person | corrected | 2026-08-12 |
| 11 | Scenario 1's closing satisfaction-survey line previously called it "the standard resolution prompt and satisfaction survey" without naming which mechanism sends it | this file's chapter-02/04/10 rows distinguishing Chatwoot's native per-inbox CSAT (on for all four inboxes, what a customer actually receives) from the assistant's own `LIFECYCLE_SURVEY_ENABLED`-gated rating prompt (off tenant-wide). Rewritten to name native CSAT specifically and note the assistant's own survey is off | corrected | 2026-08-12 |
| 11 | Scenario 2's closing step had a supervisor reading the Dealer Escalation Turnaround table during a weekly review "to see how long that dealer took to close the case" | contradicts this file's chapter-07 central finding (Dealer Escalation Turnaround is fed by the not-yet-switched-on reporting warehouse and shows fixed illustrative figures, not this account's real timings). Rewritten so the supervisor does not read this case's actual turnaround off that table and checks the case itself instead | corrected | 2026-08-12 |
| 11 | Scenario 3 (RSA to Customer 360) said "where the DMS/TSP connection is configured, the same Customer 360 result also shows that vehicle's service history" | contradicts `platform/config.py`'s `dms_mock_client_enabled: bool = False` (absent from both env dumps) — Phase 1 ships no real DMS adapter, so `dms_client=None` on this tenant and every search reads "Not connected," never a service history. Rewritten to state the block shows "Not connected" on this tenant today | corrected | 2026-08-12 |
| 11 | Scenario 5 (weekly reporting routine) had the operations lead reading Case Volume, Departments & PIC, Call Centre & SLA Performance, Case Aging, and Dealer Escalation Turnaround and drawing conclusions from them ("any dealer whose average turnaround has crept up") for a client deck | contradicts this file's chapter-07 central finding: every one of those sections (all but Per-Case Detail) reads from the not-yet-connected reporting warehouse and shows fixed illustrative figures, with no on-page indicator saying so. Rewritten: the operations lead reads Per-Case Detail (the one live section) for real cases, treats the rest as placeholders, and draws no client-facing conclusion from them until the connection goes live | corrected | 2026-08-12 |
| 11 | Scenario 6 (new agent onboarding) listed Dian's visible pages on sign-in as only "Conversations, Contacts, and Knowledge" | incomplete against this file's chapter-09 row restoring **My status** as the deliberate agent-level exception among admin-gated pages (`presence.set_own_status` granted to the default Agent role). Added to the list of what a newly onboarded agent sees | corrected | 2026-08-12 |
| 11 | Scenario 15 (case-taxonomy dropdowns): the conversation's five custom-attribute dropdowns (Case Type/Category/Subcategory/Detail/Vehicle Model), their cascade behaviour (only Category→Subcategory→Detail cascades), and their `<Division>: <Level 1>[: <Level 2>]` prefix convention, walked step by step | source: this file's chapter-05 rows (live `CustomAttributeDefinition` counts 3/8/89/246/4; `0036`/`0050` cascade + prefix logic) — the scenario describes the agent-facing conversation dropdowns, a distinct surface from the admin-only Case Taxonomy page (chapter-05/09 rows), and a clarifying clause was added to say so explicitly rather than let the two surfaces blur together under "taxonomy dropdowns" | verified | 2026-08-12 |
| 11 | Scenarios 4, 7, 8, 9, 10, 11, 12, 13, 14, 16 re-walked against every chapter-02/04/05/06/07/09/10 correction in this ledger | no contradiction found against any settled row — scenario 4's FAQ/Playground/live-answer flow, 7's private-note customer-reply mechanism, 8's dealer-group editing, 9's SLA breach note+email, 10's SLA threshold editing, 11's template editing with the inbound-auto-ack caveat, 12/13's WhatsApp/web escalation-label limitation (precise, matches ch02/09/10 wording), 14's after-hours no-bypass correction, and 16's AI-suggested-department flow (never auto-applies, never suggests a PIC-less department) all still hold as written | verified | 2026-08-12 |
| 13 | The Integration Overview chapter's WhatsApp "Example scenario" said the AI assistant "drafts a reply for an agent to review and send," describing the Suggest-mode flow | same contradiction as the chapter-11/Scenario-1 row above (`AGENT_MODE=auto` tenant-wide default). Rewritten: the assistant answers directly with no review step, since this inbox runs in Auto mode; a human only picks up when the customer needs something arranged | corrected | 2026-08-12 |
| 13 | DMS/TSP section and summary-table row implied a configurable, testable, potentially-live connection ("clicks Test connection to confirm it's reachable," "once enabled… shows that vehicle's service history") | contradicts `backend/apps/backend/src/chatbot/platform/config.py` lines 186-213 (`dms_mock_client_enabled: bool = False`, comment: "Phase 1 ships no real DMS adapter… the ONLY thing that can put data in that block… is fabricated demo records") and `main.py` lines 442-466 (`dms_client` is `None` unless the mock flag is set — the admin-saved provider/base-URL/credential config is never consulted by any live client). Raw capture confirms `DMS_MOCK_CLIENT_ENABLED` absent from both containers' env (default False applies). Section rewritten: no live adapter exists in this build at all; the admin form's saved values currently have no effect; on this tenant the block always reads "Not connected"; if a future demo mode is switched on, the block would read "Mock data" with fabricated records, never real ones | corrected | 2026-08-12 |
| 13 | **Extra webhook routes by prefix scan** (`/webhooks/zendesk`, `/webhooks/zendesk-support`, `/webhooks/zendesk-handback`, `/webhooks/zendesk-email`, `/webhooks/zendesk-sla-escalation`, `/webhooks/sunshine`, `/webhooks/twilio-whatsapp`) exist in the backend's openapi listing but are not documented in chapter 13 as customer-facing channels | source: `backend/apps/backend/src/chatbot/platform/config.py` — `crm_provider: Literal["chatwoot","zendesk"] = "chatwoot"`; raw capture confirms `CRM_PROVIDER` is absent from both containers' env (default `chatwoot` applies, not `zendesk`); `main.py` lines 480-518 — the `ZendeskAdapter` (the only consumer of `zendesk_webhook`/`sunshine_webhook`/`zendesk_support_webhook`/`zendesk_handback_webhook`/`zendesk_email_webhook`) is constructed only `if settings.crm_provider == "zendesk"`, so it is never constructed on this tenant; `router.py`'s `twilio_whatsapp_webhook` is a separate, unrelated ingress from the live WhatsApp channel, which runs through Chatwoot's own native `Channel::TwilioSms` inbox (per this file's `### native CSAT and greeting per inbox` capture) and the `agent/` service's Chatwoot webhook, not through this backend endpoint. These routes are leftover surface from the vendored `proton-conversational-ai` backend's original multi-CRM design (per the root `CLAUDE.md`), always mounted regardless of provider, but functionally inert and not reachable/meaningful for this Chatwoot-only tenant — deliberately not documented as a channel or integration in chapter 13 | verified | 2026-08-12 |
| 14 | Added glossary entries for Translate, FAQ suggestion strip, Inbound alert, Agent status, Case Taxonomy admin page, and Agent Channel Priorities — terms an operator meets in the UI that had no entry before | source: matched against each term's own settled chapter section — `02-conversations.md`'s Translate/FAQ suggestion strip/Inbound alerts sections (this file's chapter-02 rows on `TRANSLATION_ENABLED`/`FAQ_SUGGESTION_POPUP_ENABLED`/`INBOUND_ALERTS_ENABLED`), `09-administration.md`'s Agent Availability & Workforce Dashboard section (My status/named statuses) and Inboxes section (Agent Channel Priorities table, patch `0024`+`0063`+`0064`), and `05-cases.md`'s Case categorisation section (Case Taxonomy admin page, patch `0060`, `sync_taxonomy_to_chatwoot` rewriting three of five option lists in full) | verified | 2026-08-12 |
| 14 | DMS/TSP glossary entry said the CRM's dealer vehicle/service-history system was "optionally connected to the CRM" | same finding as this file's chapter-13 DMS row (`dms_mock_client_enabled: bool = False`, no real adapter shipped). Rewritten: a placeholder connection point exists but no live adapter has been built, and the block reads "Not connected" today | corrected | 2026-08-12 |
| 12 | Chapter 12 (Channel Playbooks) re-walked against every chapter-02/04/05/06/07/09/10 correction in this ledger, plus the AGENT_MODE/escalation-channel/DMS/reporting landmines named in this task's brief | no contradiction found — the chapter already states Auto-mode-consistent Scenario A/B framing with no suggest-mode private-note-review claim on any channel, the escalate-label channel limitation matches ch02/09/10's precise wording, the Customer 360 DMS caveat ("No dealer management system is connected") already matches this task's chapter-13 finding, and the chapter makes no claim about reporting-page numbers. No edit made to this file — leaving text unchanged was the correct action, not an oversight | verified | 2026-08-12 |

## Raw capture

```
=== chatwoot .git_sha ===
0866fda
=== chatwoot image ===
asia-southeast1-docker.pkg.dev/lv-playground-genai/proton-images/proton-chatwoot:v4.15.1-custom-rc6
=== SPA feature list (rendered login page) ===
features: "ai_assist,nav_menu,copilot,knowledge,inbound_alerts,faq_suggestion_popup".split(',').filter(Boolean)
=== backend openapi paths ===
/
/admin/audit
/admin/customer360/search
/admin/escalation/dealers
/admin/escalation/dealers/{dealer}
/admin/escalation/pics
/admin/escalation/pics/{department}
/admin/integrations/dms
/admin/integrations/dms/test
/admin/sla-policy/default
/admin/sla-policy/inbox/{inbox_id}
/admin/taxonomy/coverage
/admin/taxonomy/node
/admin/taxonomy/node/{key}/retire
/admin/taxonomy/tree
/admin/workforce
/alerts/rules/defaults
/alerts/rules/defaults/{event}
/alerts/rules/mine
/alerts/rules/mine/{event}
/assist/ask
/assist/copilot
/assist/suggest
/assist/summarize
/assist/translate
/authz/check
/authz/permission-registry
/authz/permissions
/authz/roles
/authz/roles/{role_id}/assign
/authz/roles/{role_id}/permissions
/authz/roles/{role_id}/permissions/{permission_key}
/authz/roles/{role_id}/users
/calls/{conversation_id}/recording
/cases/{conv_id}/fields
/cases/{ticket_id}/audit
/chat/csat
/chat/nps
/chat/stream/{session_id}
/chat/turn
/escalation/acknowledge
/escalation/contacts
/escalation/departments
/escalation/notify
/healthz
/kb/assistants
/kb/assistants/{assistant_id}
/kb/documents
/kb/faq
/kb/faq/bulk
/kb/faq/{entry_id}
/kb/feedback
/kb/inboxes
/kb/inboxes/{inbox_id}
/kb/inboxes/{inbox_id}/timing
/kb/knowledge
/kb/knowledge/file
/kb/knowledge/text
/kb/knowledge/{document_id}
/kb/scenarios
/kb/scenarios/{scenario_id}
/kb/settings
/kb/suggest
/kb/tools
/kb/tools/builtins/{name}
/kb/tools/{slug}
/metrics/after-hours
/metrics/ai-cost
/metrics/anomalies
/metrics/anomalies/hourly
/metrics/by-tag
/metrics/callcenter
/metrics/case-aging
/metrics/case-aging/export
/metrics/control-items
/metrics/dashboard
/metrics/dealer-escalation
/metrics/dealer-escalation/export
/metrics/departments
/metrics/departments/export
/metrics/export
/metrics/freshness
/metrics/lifecycle
/metrics/sla-buckets
/metrics/sla-buckets/export
/metrics/volume-by-type
/metrics/volume-by-type/export
/qa/label
/routing/agents
/routing/assign
/routing/presence/status
/routing/presence/statuses
/routing/presence/statuses/{key}
/routing/priorities
/routing/priorities/{agent_id}
/rsa/incidents
/rsa/incidents/aggregate
/rsa/incidents/export
/rsa/incidents/{incident_id}
/tasks/mine
/voice/phone/incoming
/voice/phone/token
/voice/tts
/voice/turn
/webhooks/chatwoot
/webhooks/phone/recording-status
/webhooks/sunshine
/webhooks/twilio-whatsapp
/webhooks/zendesk
/webhooks/zendesk-email
/webhooks/zendesk-handback
/webhooks/zendesk-sla-escalation
/webhooks/zendesk-support
=== agent env ===
AGENT_MODE=auto
ASSIST_MEDIA_UNDERSTANDING_ENABLED=true
BOUNCE_HANDLING_ENABLED=true
CHAT_AGENT_ENABLED=true
DEPT_SUGGESTION_ENABLED=true
EMAIL_AUTOACK_ENABLED=false
EMAIL_ESCALATION_ACK_ENABLED=true
EMAIL_ESCALATION_ENABLED=true
ESCALATION_EMAIL_ENABLED=true
ESCALATION_REPLY_DRAFT_ENABLED=true
ESCALATION_REPLY_LINKING_ENABLED=true
FAQ_SUGGESTION_POPUP_ENABLED=true
INBOUND_ALERTS_ENABLED=true
KNOWLEDGE_PG_ENABLED=true
LIFECYCLE_DISCLAIMER_ENABLED=false
LIFECYCLE_ENABLED=true
LIFECYCLE_SURVEY_ENABLED=false
PHONE_LANGUAGE_NUDGE_ENABLED=true
RBAC_ENABLED=true
ROUTING_ENABLED=true
RSA_ENABLED=true
SLA_ALERT_EMAIL_ENABLED=true
SLA_ALERT_NOTE_ENABLED=true
SLA_ENGINE_ENABLED=true
SLA_INBOX_IDS=4
TRANSLATION_ENABLED=true
WHATSAPP_MEDIA_UNDERSTANDING_ENABLED=true
ZAMMAD_TICKETING_ENABLED=false
=== backend env ===
AGENT_MODE=auto
ASSIST_MEDIA_UNDERSTANDING_ENABLED=true
BOUNCE_HANDLING_ENABLED=true
CHAT_AGENT_ENABLED=true
DEPT_SUGGESTION_ENABLED=true
EMAIL_AUTOACK_ENABLED=false
EMAIL_ESCALATION_ACK_ENABLED=true
EMAIL_ESCALATION_ENABLED=true
ESCALATION_EMAIL_ENABLED=true
ESCALATION_REPLY_DRAFT_ENABLED=true
ESCALATION_REPLY_LINKING_ENABLED=true
FAQ_SUGGESTION_POPUP_ENABLED=true
GEMINI_MODEL=gemini-2.5-flash
INBOUND_ALERTS_ENABLED=true
KNOWLEDGE_PG_ENABLED=true
LIFECYCLE_DISCLAIMER_ENABLED=false
LIFECYCLE_ENABLED=true
LIFECYCLE_SURVEY_ENABLED=false
PHONE_LANGUAGE_NUDGE_ENABLED=true
PRESENCE_CUSTOM_STATUSES_ENABLED=true
PRESENCE_TRACKING_ENABLED=true
RBAC_ENABLED=true
ROUTING_ENABLED=true
RSA_ENABLED=true
SLA_ALERT_EMAIL_ENABLED=true
SLA_ALERT_NOTE_ENABLED=true
SLA_ENGINE_ENABLED=true
SLA_INBOX_IDS=4
TAXONOMY_ADMIN_ENABLED=true
TRANSLATION_ENABLED=true
WHATSAPP_MEDIA_UNDERSTANDING_ENABLED=true
ZAMMAD_TICKETING_ENABLED=false
=== rails env (x-chatwoot-env passthrough only) ===
FAQ_SUGGESTION_POPUP_ENABLED=true
INBOUND_ALERTS_ENABLED=true
PROTON_FEATURES=ai_assist,nav_menu,copilot,knowledge
```

### BigQuery probe (Step 2)

```
$ gcloud config get-value project
lv-playground-genai

$ bq ls --format=pretty | grep -i proton
| demo_proton                     |

$ bq ls --format=pretty demo_proton
+---------------------------+-------+--------+-------------------+------------------+
|          tableId          | Type  | Labels | Time Partitioning | Clustered Fields |
+---------------------------+-------+--------+-------------------+------------------+
| conversations             | TABLE |        |                   |                  |
| qa_labels                 | TABLE |        |                   |                  |
| turn_events               | TABLE |        |                   |                  |
| v_bounce_rate             | VIEW  |        |                   |                  |
| v_csat                    | VIEW  |        |                   |                  |
| v_fallback_rate           | VIEW  |        |                   |                  |
| v_nps                     | VIEW  |        |                   |                  |
| v_quality                 | VIEW  |        |                   |                  |
| v_resolution_split        | VIEW  |        |                   |                  |
| v_speed_of_response       | VIEW  |        |                   |                  |
| v_volume_by_month_channel | VIEW  |        |                   |                  |
+---------------------------+-------+--------+-------------------+------------------+
```

The full `bq ls --format=pretty` project-wide listing was long (100+
datasets, none else proton-named); only the `proton`-matching row and the
`demo_proton` dataset's contents are reproduced above per the brief's
two-attempt cap on BigQuery hunting.

### native CSAT and greeting per inbox (added 2026-08-12, Task 6 fix round;
corrected 2026-08-12, Task 7 fix round 1)

Read-only query of Chatwoot's own `Inbox.csat_survey_enabled` and
`Inbox.greeting_enabled` (Chatwoot's native per-inbox CSAT and greeting
toggles — both distinct from the agent/backend's own `LIFECYCLE_SURVEY_ENABLED`/
`LIFECYCLE_DISCLAIMER_ENABLED`-gated messages). Query:

```ruby
Inbox.all.each { |i| puts "[#{i.id}] #{i.name} | channel=#{i.channel_type} | csat_survey_enabled=#{i.csat_survey_enabled} | greeting_enabled=#{i.greeting_enabled}" }
```

Run against the live tenant, 2026-08-12:

```
[1] Proton API    | Channel::Api       | csat_survey_enabled=true | greeting_enabled=false
[2] Website Demo  | Channel::WebWidget | csat_survey_enabled=true | greeting_enabled=false
[3] Twilio Proton | Channel::TwilioSms | csat_survey_enabled=true | greeting_enabled=true
[4] Email         | Channel::Email     | csat_survey_enabled=true | greeting_enabled=true
```

**Correction note:** this is the same query as Task 6's fix round; the
version of this subsection committed on 2026-08-12 by Task 6 transcribed
only the `csat_survey_enabled` column and dropped `greeting_enabled` from
the record, even though the live query captured both. Task 7's brief
asserted the greeting column was already captured here; it was not, Task 7
correctly flagged the gap instead of fabricating a value, and this
subsection was then corrected with the query's full, verbatim output above.
Nobody should conclude from the earlier version that the greeting column
was never queried — it was queried, just not recorded.

All four inboxes have native CSAT **on** — this is the survey a customer
actually receives on resolve, and it's what feeds the `v_csat` BigQuery
view above — a *different* mechanism from the AI assistant's own
`LIFECYCLE_SURVEY_ENABLED`-gated rating prompt, which is off (see the Raw
capture agent/backend env block earlier in this file).

Native greeting is **on** for Twilio Proton (WhatsApp) and Email, **off**
for Proton API and Website Demo — again a different mechanism from the AI
assistant's own `LIFECYCLE_DISCLAIMER_ENABLED`-gated Welcome message, which
is off tenant-wide. This query shows only the on/off state, not the
greeting's configured text. Chapters 4 and 10 make lifecycle-message claims
and should use both distinctions rather than re-deriving them.

### live case-taxonomy custom attribute definitions (added 2026-08-12, controller probe for Task 8)

Read-only query of Chatwoot's own `CustomAttributeDefinition` table — i.e. what
an operator actually sees in the conversation sidebar dropdowns, as distinct
from what `docs/client-materials/RFP 2026_028/case-categorisation.json` says
*should* be provisioned. Chapter 5 describes the former.

Query:

    CustomAttributeDefinition.order(:attribute_key).each do |d|
      vals = d.attribute_values || []
      puts "#{d.attribute_key} | model=#{d.attribute_model} | display=#{d.attribute_display_type} | values=#{vals.length}"
    end

Verbatim output:

```
case_category | model=conversation_attribute | display=list | values=8
case_detail | model=conversation_attribute | display=list | values=246
case_subcategory | model=conversation_attribute | display=list | values=89
case_type | model=conversation_attribute | display=list | values=3
vehicle_model | model=conversation_attribute | display=list | values=4
```

All five are `list` type on the conversation. The counts match the figures v3
quoted (3 / 8 / 89 / 246 / 4) exactly, so chapter 5's taxonomy numbers are
confirmed against the live tenant rather than against the source JSON.
