# Placeholder image so `deploy/docker-compose.yml` (Task 1) has a valid build
# context. Task 3 replaces this with the real FastAPI integration service.
FROM python:3.12-slim
CMD ["sleep", "infinity"]
